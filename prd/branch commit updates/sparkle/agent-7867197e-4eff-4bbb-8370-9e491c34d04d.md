# Branch Progress: sparkle/agent-7867197e-4eff-4bbb-8370-9e491c34d04d

## Progress Update as of [2026-07-24 22:15 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update
Merged PR #38 (`77cc471`), then handled roborev round 4 on `a8db4e4`. Round 3's `/` blocker was over-broad and silently dropped `@Alice/@Bob`, exactly as predicted in the previous entry's headline concern. **This round changed the approach rather than the character class**: the opener decision is now made from the surrounding whitespace-delimited token, so a `/` is judged in context instead of blanket-rejected. Also closed two more wrong-recipient holes (astral-script email locals, typographic hyphens) and pinned every opener trade-off with tests. Tests 58 → 76.

### Detail of changes made:
- **Approach change — this is the important part.** Three rounds of tuning `MENTION_BLOCKER` as a character class kept trading one silent-drop for one wrong-recipient. A single character genuinely cannot distinguish `medium.com/@alice` (URL) from `@Alice/@Bob` (two mentions), so `/` was removed from the class and `isUrlSlashBefore()` now decides from the token: it scans back to whitespace and treats the slash as a URL separator only if that segment contains `://` or matches `DOTTED_HOST` (`/[\p{L}\p{N}]\.[\p{L}]/u`). Result: `https://medium.com/@alice`, `mastodon.social/@alice`, `github.com/@alice` still rejected; `@Alice/@Bob` and `and/or/@Alice` now resolve.
- **Wrong-recipient: astral-script email locals (Low, pre-existing asymmetry).** The trailing boundary check was made code-point-aware in #37, but the opener check still indexed `body[i-1]`, a lone low surrogate for astral text (general category `Cs`, matching no property in the class). So `𐐨@alice.com` (U+10428 Deseret) opened a mention and emailed signer Alice. Added `codePointBefore()`, which steps back two code units past a low surrogate. Opener and trailing checks are now symmetric — that asymmetry was the actual root cause.
- **Wrong-recipient: typographic hyphens (Low).** `endsMidWord` special-cased only U+002D, so `@Jean‑Pierre` with U+2011 (non-breaking hyphen, common from paste/autocorrect) let signer "Jean" match a partial word again. Replaced with a `NAME_HYPHENS` list covering U+002D, U+2010, U+2011, advancing by `ch.length`.
- **Reverted my own over-reach (Low).** #38 added `& ! # = ? ~` to the blocker "for RFC completeness." Roborev correctly noted the set still isn't RFC-complete (`'` is deliberately excluded so `'@Alice'` resolves), so those six bought no real coverage while silently dropping `Great!@Alice`, `Really?@Alice`, `~@Alice`. Removed. The blocker is now only `/[\p{L}\p{N}\p{M}_.+%-]/u` — what a realistic address actually needs.
- **Every opener trade-off is now pinned by a test**, which was roborev's structural complaint. New `email-local characters before the @` block: a 7-case `it.each` asserting letters/dot/plus/percent/hyphen/underscore/digit before an `@` do NOT open a mention, and a 6-case `it.each` asserting `! ? ~ & # =` DO. A future round can't quietly loosen either direction.
- **Tests 58 → 76.** Above, plus `@Alice/@Bob`, `and/or/@Alice`, the astral local part, and two typographic-hyphen cases.
- **Verification:** 76/76 mentions, 7/7 render, 15/15 server comments (83 in the two lib files). `tsc --noEmit` and `eslint` clean. 11 of the new tests confirmed failing against `a8db4e4`.

### Potential concerns to address:
- **Four rounds, four sets of real findings, in one ~40-line matcher.** Rounds 1-3 were character-class whack-a-mole; each fix opened an adjacent hole. Round 4 finally changed the *shape* of the rule (token context, not one character) and made the opener/trailing checks symmetric, which is why it resolved two independent classes at once. If a fifth round finds another boundary bug, that is strong evidence to stop tuning and do the structural fix below rather than patch again.
- **The structural fix remains unbuilt and is still the right one: composer autocomplete resolving a mention to a `signerId` at write time**, persisted instead of re-derived from prose on every render. `src/components/MentionTextarea.tsx` exists but does no mention lookup. Everything above is inference from free text, which is inherently lossy; storing the resolved id makes the entire bug class impossible. Recommend doing this before further regex work.
- The character classes and the `isUrlSlashBefore` heuristic encode substantial accumulated judgement that now lives in docstrings **and** in pinning tests. Both are load-bearing; don't "simplify" either without reading the tests.
- `DOTTED_HOST` is a heuristic: a URL without a dot or scheme (e.g. `localhost/@alice`) would still open a mention. Judged acceptable — such text is rare in comments, and the failure is a spurious notification to a real signer rather than silence.
- Ambiguous mentions still fail silently in the UI (deferred since #35, unchanged).
- `dev-preview/` is untracked scratch (harness, 1.1MB `bundle.js`, `mentions.new.ts`, `pr-body.md`). Safe to delete.

---

## Progress Update as of [2026-07-24 22:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update
Merged PR #37 (`6568e71`), then handled the roborev review of `f69a203` — the **third consecutive round to find a live wrong-recipient bug in this matcher**, and again one I had just introduced: inverting the opener whitelist into a rejection set left `/` out, so `https://medium.com/@alice` notified signer Alice. Fixed that plus three more, tests 48 → 58.

### Detail of changes made:
- **Wrong-recipient bug I introduced in #37 (Medium).** `#37` replaced the opener whitelist with `EMAIL_LOCAL` rejection. `/` was not in the set, so the `@` in a URL path opened a mention: `https://medium.com/@alice`, `https://mastodon.social/@alice`, `github.com/@alice` all notified signer `Alice`. The old whitelist had rejected these because `/` was not an allowed opener — so this was a **regression** #37 introduced while fixing the opener over-rejection. Renamed the constant to `MENTION_BLOCKER` (it is no longer purely about email locals) and added `/` plus the RFC-legal local-part characters `& ! # = ? ~`. The apostrophe is deliberately still excluded so a quoted `'@Alice'` resolves; documented that this leaves `bob'smith@alice.com` uncaught.
- **Silent-miss bug (Low).** #37's short-name search applied `length >= 2` *after* `find(isNameLike)`, so a leading single-letter token was selected and then rejected, dropping the candidate entirely. `"J Erika Anderson"` → `@Erika` notified nobody. (`"J. Erika Anderson"` worked only because the period incidentally tripped `isNameLike`.) The length rule now lives **inside** the predicate: `.find((t) => t.length >= 2 && isNameLike(t))`.
- **Hyphen over-rejection (Low).** #37 put `-` in `WORD_CHAR` to stop `@Jean-Pierre` matching signer "Jean", but that silently dropped mentions where the hyphen is punctuation: `@Alice--`, `@Alice-` at a line break. Same over-rejection #37 had criticized on the opener side, just on the trailing side. Removed `-` from `WORD_CHAR` and added `endsMidWord()`, which treats a hyphen as intra-name **only when a word char follows it** — so `@Jean-Pierre` stays rejected while `@Alice--` and `@Alice-` resolve.
- **NFC/NFD limitation now pinned by tests (Low).** The docstring documented the fail-safe but no test asserted it. Added two: an NFD-stored `displayName` against an NFC body asserting `[]` (the documented safe failure), and a same-normalization pair asserting a match. If anyone later adds normalization, these tell them exactly what behavior they are changing.
- **Tests 48 → 58.** New `URLs` block (3-case `it.each` plus "still matches a mention on the line after a URL"), `hyphen as punctuation` block (3 cases), the single-letter-initial case, and the two normalization cases.
- **Verification:** 58/58 mentions, 7/7 render, 15/15 server comments. `tsc --noEmit` and `eslint` clean. 6 of the 10 new tests confirmed failing against `f69a203`; the other 4 pass on both by design — they pin existing behavior (the NFD fail-safe, `@Jean-Pierre` still rejected) rather than fixing it. Baseline comparison again done with `git show <sha>:<path>`, not `git stash`.

### Potential concerns to address:
- **THE HEADLINE CONCERN: three consecutive roborev rounds, three live wrong-recipient bugs, all in the same 3 lines of boundary logic.** The sequence was accented → hyphenated/NFD → URL-path, and twice the fix for one class introduced a new bug in an adjacent class. Each individual fix is correct and tested, but the pattern says the *approach* is the problem: hand-rolled character-class boundary matching in a path that sends email is not converging. **The real fix is composer autocomplete that resolves a mention to a `signerId` at write time**, storing the resolved id instead of re-deriving it from prose on every render. `src/components/MentionTextarea.tsx` already exists but does no mention lookup. Strongly recommend that before any further tuning of these regexes — it would delete this entire class of bug.
- The matcher is now correct on every case tested, but the character classes encode a lot of accumulated judgement (which punctuation is a name boundary, which can precede an `@`, which is RFC-legal in an email local part). That knowledge lives only in the constants' docstrings and the tests. Treat those docstrings as load-bearing.
- Ambiguous mentions still fail silently in the UI (deferred since #35, unchanged).
- `dev-preview/` is untracked scratch: the before/after harness (with a 1.1MB generated `bundle.js`), `mentions.new.ts` used for baseline swaps, and `pr-body.md`. Safe to delete.

---

## Progress Update as of [2026-07-24 21:45 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update
Merged PR #35 (`e1216c4`), then acted on the roborev review of `cc3af02`, which found three more real defects — including a **regression I introduced in #35**: skipping non-name-like tokens meant `"Dr. Erika Anderson"` got no short-name candidate at all, so `@Erika` notified nobody. That is precisely the silent-miss failure this module exists to prevent. Also closed two remaining wrong-recipient holes (hyphenated and NFD-decomposed names) and replaced the over-restrictive opener whitelist. Tests 33 → 48; all 12 new cases verified failing against `cc3af02`.

### Detail of changes made:
- **Regression fixed (roborev finding 3).** `#35` changed the short-name candidate to `name.split(" ")[0]` gated on `isNameLike()`. For `"Dr. Erika Anderson"` token 0 is `"Dr."`, which fails the gate, so the signer got **no** short-name candidate and `@Erika` resolved to nobody. Now `name.split(" ").find(isNameLike)` scans forward to the first name-like token: `"Dr. Erika Anderson"` → `erika`, `"Anderson, Erika"` → `erika`. A bare `@Dr.` still matches nothing. This is the one to remember — the #35 fix for a cosmetic issue (`@Dr.` being a mention) silently broke the primary use case.
- **Wrong-recipient hole: hyphenated names (Medium).** `WORD_CHAR` was `/[\p{L}\p{N}_]/u`, and `-` is not in it, so with only signer `Jean Dupont`, the body `@Jean-Pierre` matched candidate `jean` and **notified Jean Dupont**. Same for `Anne` in `@Anne-Marie`. Hyphenated given names are more common in this signer population than the `Anaïs` case that motivated #35. `WORD_CHAR` is now `/[\p{L}\p{N}\p{M}_-]/u`.
- **Wrong-recipient hole: NFD text.** Combining marks are `\p{M}`, not `\p{L}`, so pasted decomposed text (where `ä` is `a` + U+0308) let signer `Ana` match inside `@Anäs` exactly as before #35. Covered by the `\p{M}` addition. The new test asserts `nfd.normalize("NFC") !== nfd` first, so it can't silently degrade into a precomposed (and therefore vacuous) test.
- **Astral letters.** `WORD_CHAR.test(body[end])` tested a single UTF-16 code unit, so an astral letter at the boundary was a lone surrogate and never counted as a word char. Extracted `isWordCharAt()`, which tests the full code point via `codePointAt` + `String.fromCodePoint`.
- **Opener whitelist replaced (Low).** #35's `MENTION_OPENER = /[\s([{<"'‘“]/u` fixed the email hole but silently dropped ordinary mentions: `Hi,@Alice`, `cc:@Alice`, `—@Alice`, `»@Alice`, `¿@Alice` were all non-mentions, and quote coverage was asymmetric (`‘`/`“` but not `’`/`”`). Inverted to reject only what an email local part can contain: `EMAIL_LOCAL = /[\p{L}\p{N}\p{M}_.+%-]/u`. Strictly better — still rejects `bob@`, `josé@`, `田中@`, `bob.smith@`, and `sentence.@Alice`, while accepting all punctuation openers without an enumerated list.
- **Deliberately NOT done: Unicode normalization.** Roborev suggested `.normalize("NFC")` on both body and names. Declined and documented as a KNOWN LIMITATION in the module docstring: an NFD-typed `@María` won't match an NFC-stored `María`. That direction fails **safe** (notifies nobody, not the wrong person), whereas normalizing shifts every match offset and the renderer slices the original body — a real risk of introducing an offset bug to fix a convenience gap.
- **Tests 33 → 48.** New `hyphenated and composed names` block (hyphen boundary both ways, NFD with guard, astral letter, apostrophe still matching) and `honorifics` block (honorific-prefixed name reachable by short name, bare honorific still refused, comma-inverted name). `mention openers` extended with a 6-case `it.each` for punctuation.
- **Verification:** 48/48 mentions, 7/7 render, 15/15 server comments. `tsc --noEmit` and `eslint` clean. All 12 new tests confirmed **failing against `cc3af02`** and passing after — done with a file swap through `git show cc3af02:<path>`, **not** `git stash`.

### Potential concerns to address:
- **Two rounds of roborev both found live wrong-recipient bugs in this matcher**, each in the boundary character class. The pattern is clear: character-class boundary logic in a notification path is easy to get subtly wrong, and each fix revealed another class of name (accented → hyphenated → decomposed). Composer autocomplete that resolves a mention to a `signerId` at write time would make all of this obsolete and is the real fix; the brainstorm doc already lists it.
- Ambiguous mentions still fail silently in the UI (unchanged, deferred from #35).
- The NFD limitation above is now a documented, deliberate gap rather than an unknown one.
- `dev-preview/` holds the harness plus a scratch `mentions.new.ts` copy used for the baseline comparison, and a `pr-body.md`. Untracked; delete when done with the harness.

---

## Progress Update as of [2026-07-24 21:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update
Landed PR #33 to `main` (merge commit `c65fc76`), then acted on the roborev review of `619a3fb`, which found that the word-boundary guard in the just-shipped fix was **ASCII-only** — so it still permitted the exact wrong-recipient bug the commit claimed to close. Hardened the matcher for Unicode and untidy display names, added 13 tests, and confirmed the new cases fail against the shipped code. Also had to clean up a null-byte corruption I introduced into `mentions.ts`.

### Detail of changes made:
- **PR #33 merged.** No GitHub Actions workflows exist in this repo; the only status check is the Vercel deploy (which runs `next build`). It went SUCCESS, mergeStateStatus CLEAN, merged with `gh pr merge 33 --merge` (merge commit, per orchestrator policy).
- **Roborev finding 1 (Medium) — fixed. This was a real wrong-recipient bug.** `WORD_CHAR` was `/[A-Za-z0-9_]/`, which does not consider `ï`/`ö`/CJK a word character. Consequences, both confirmed by test against the shipped code:
  - Trailing side: signer `Ana` matched inside `@Anaïs` (boundary check saw `ï` as a non-word char and let the partial match through). Same class for `Jo` in `@Jörg`, `Mar` in `@María`.
  - Leading side: `josé@alice.com` and `田中@alice.com` both notified signer `Alice`, because the "@ starts a word" check only blocked ASCII word chars before the `@`. The one existing test used all-ASCII `bob@alice.com` and so passed.
  - Fix: `WORD_CHAR` is now `/[\p{L}\p{N}_]/u`. The leading check was replaced with a stricter, more predictable rule: a mention's `@` may only follow whitespace or opening punctuation (`MENTION_OPENER = /[\s([{<"'‘“]/u`). That rejects *every* email local part regardless of script, and also fixes dotted locals like `bob.smith@alice.com`. Deliberate trade-off recorded in a code comment: `"end of sentence.@Alice"` is no longer a mention. A false negative is preferable to mailing the wrong person.
- **Roborev finding 1, second half — fixed.** `end` was computed from `candidate.length` where the candidate was the *lowercased* name, so a name whose case folding changes length (e.g. `İ` → two code units) produced a `matchEnd` misaligned with the original body, corrupting the render slice. Candidates are now `{ key: lowercased, len: ORIGINAL length }`, and matching slices the body by `len` then lowercases the slice — keeping all offsets in original-body space. Candidates are keyed on `(key, len)` so two spellings folding to the same key remain independently scannable.
- **Roborev finding 3 (Low) — fixed.** Display names are user-entered and untidy. Internal whitespace is now collapsed (`replace(/\s+/g, " ")`), so a name stored as `"Erika  Anderson"` is still matchable. First-name tokens must pass `isNameLike()` (contain a letter, not end in `.,;:`), so `"Dr. Erika Anderson"` no longer makes a bare `@Dr.` a real mention and `"Anderson, Erika"` no longer contributes `anderson,`.
- **Roborev findings 2 & 4 (Low) — partially addressed.** Added the missing tests: duplicate *full* display names resolving to nobody, and the ambiguous-cursor-advance case (signers `Erika Anderson`/`Erika Smith` plus a signer `Ka`, against `@Erika hi`, proving the `ka` tail isn't re-matched). **Not done:** returning ambiguous matches as `signerId: null` so the renderer can visibly flag "we couldn't tell who you meant." That's a UI/UX change with a product decision attached, not a bug fix — left for the mention-autocomplete work instead.
- **Roborev finding 5 (Low) — closed, not actionable now.** Index construction per `parseMentions` call. Roborev itself notes it's "roughly on par with the old per-call sort, so not a regression." The proposed fix (a memoized `buildMentionIndex` threaded through `CommentView`/`CommentsColumn`) is a cross-component refactor well outside a bug fix. Deferred deliberately.
- **Tests:** `comments.mentions.test.ts` 20 → 33. New blocks: `non-ASCII names` (accented/CJK boundaries, accented name matched in another case, non-ASCII and dotted email locals) and `untidy display names` (doubled whitespace, titles, comma-terminated tokens, full title-prefixed name), plus `mention openers` (parenthesized, after newline) and two additions to `refuses to guess`.
- **Verification:** 33/33 mentions + 7/7 render + 15/15 `tests/server/comments.test.ts` = 55 passing. `tsc --noEmit` clean, `eslint` clean. Critically, the three headline cases were re-run against the **shipped** `mentions.ts` and all three failed, confirming these are real fixes and not tests written to fit existing behavior.

### Potential concerns to address:
- **I introduced null bytes into `src/lib/comments/mentions.ts`.** The composite-key separator was emitted as a literal `U+0000` byte rather than the intended delimiter, twice. It was functionally harmless (NUL is legal in a JS string, and tests passed) but it made git treat the file as **binary** — no readable diff, and `grep` silently failing on it. Fixed by writing the separator as the escape `"\u0000"` (a NUL separator is actually the correct choice, since it cannot occur in a display name; only the literal byte was wrong). **If a source file ever shows as `Bin N -> M bytes` in `git diff --stat`, check for null bytes immediately.**
- **`git stash` mangled this worktree's index a second time**, staging every tracked file as deleted, and a `git checkout HEAD -- <file>` in the same recovery sequence silently discarded uncommitted work that had to be rewritten. Recovered with `git reset`. This is now twice. **Do not use `git stash` in this worktree** — use a scratch commit or a second worktree to compare against a baseline.
- The stricter `MENTION_OPENER` rule means a mention immediately following punctuation (`"...decided.@Alice"`) is silently not a mention. Predictable and safe, but invisible to the author — another argument for composer autocomplete.
- Ambiguity still fails silently in the UI: an ambiguous `@Erika` renders as plain text with no indication the mention went nowhere. Tracked above as the deferred half of finding 2.
- `dev-preview/` (the before/after harness) is untracked and deliberately uncommitted. It contains a 1.1MB generated `bundle.js`. Delete it or add it to `.gitignore` if it becomes a nuisance.

---

## Progress Update as of [2026-07-24 20:45 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update
Shipped the `parseMentions` fix identified in the previous entry's audit. Rewrote the matcher to be case-insensitive, to accept first names, and to refuse to guess when a name is ambiguous. Verified in a browser harness that the old code was not merely *missing* mentions — on `@Erika` against a signer list containing both "Erika Anderson" and "Erik", it notified **Erik**, the wrong person. That reframes this from a missed-notification bug to a wrong-recipient bug.

### Detail of changes made:
- **`src/lib/comments/mentions.ts` rewritten.** Old algorithm: sort signers by display-name length desc, then require an exact, exactly-cased prefix match of the full display name after `@`. New algorithm:
  - Builds a candidate index of lowercased names: each signer's **full display name** plus their **first name** (first names only when ≥2 chars and different from the full name — single-char first names are too collision-prone).
  - Full names are layered *over* first names in the index, so a signer literally named "Dan" wins `@Dan` over another signer's first name "Dan(iel)". Test covers this.
  - A name claimed by two different signers is stored as `null` (ambiguous) and matches **nobody** rather than guessing. `@Erika` with two Erikas notifies no one; `@Erika Smith` resolves fine.
  - Requires the `@` to start a word (`i === 0 || !WORD_CHAR.test(body[i-1])`), which is what makes `bob@alice.com` stop matching signer "Alice".
  - Requires the match to *end* on a word boundary, which is what stops signer "Erik" from matching inside `@Erika`. This is the guard against the wrong-recipient bug above.
  - On an ambiguous match the cursor still advances past the consumed name (`i = end`) so we don't rescan inside it and pick up a tail fragment.
  - Exported a named `ParsedMention` interface; the return shape is unchanged, so `render-mentions.tsx` and `comments.ts` needed no signature changes.
- **`src/lib/comments/render-mentions.tsx`:** the highlight span now renders `body.slice(matchStart, matchEnd)` instead of `@{mention.displayName}`. Necessary because matching is now case-insensitive and accepts first names — echoing the canonical name would have silently rewritten the author's text (typing `@bob` would have displayed as `@Bob Smith`). `displayName` on the parse result is still the canonical name, which is what the email path wants.
- **Tests:** `tests/lib/comments.mentions.test.ts` grew from 9 to 20 tests, in two new describe blocks — `hand-typed names` (case-insensitivity, first-name matching, full-name precedence, apostrophe boundary) and `refuses to guess` (ambiguous first names, shorter-name-inside-longer, email addresses, blank display names). Added one test to `tests/lib/comments.render-mentions.test.tsx` asserting the span shows typed text. All 9 original tests kept and still pass unmodified.
- **Verification run:** `vitest run` on the three mention-touching files → 42/42 pass. `tsc --noEmit` → clean. `eslint` on the changed files → clean. Full suite: 194/198, with 4 failures in `tests/server/selfie.review.test.ts` and `tests/server/revoke.test.ts`; confirmed pre-existing by stashing my changes and re-running those two files alone (8/8 pass in isolation) — they are 15s-timeout flakes under full-suite parallel load with pglite, unrelated to this change.

### Potential concerns to address:
- **The 4 selfie/revoke test failures are a real flake, just not mine.** They pass in isolation and time out at 15s under full-suite load. Worth either raising `testTimeout` for the pglite-backed selfie tests or reducing suite parallelism, otherwise `pnpm test` stays red and stops being a useful gate.
- **`pnpm test` does not currently work in this worktree** — `pnpm install` exits 1 on `ERR_PNPM_IGNORED_BUILDS` (unapproved build scripts for sharp, esbuild, @clerk/shared, unrs-resolver), so the `pretest` install check fails before vitest runs. Workaround used here: `./node_modules/.bin/vitest run` directly. Fix is `pnpm approve-builds` once, or an `allowBuilds` block in `pnpm-workspace.yaml`. Note: running `pnpm test` **rewrote `pnpm-workspace.yaml`** with placeholder `allowBuilds` entries ("set this to true or false"); that edit was reverted here, but be aware it can reappear.
- **First-name matching widens the blast radius of a mention.** With a large signer list, common first names will go ambiguous and silently notify nobody — which is the safe direction, but it's invisible to the comment author. A composer with mention autocomplete (`src/components/MentionTextarea.tsx` exists but does not do mention lookups) would remove the guesswork entirely and is the real long-term fix.
- Ambiguity is resolved against the *whole* signer list, so adding a new signer can retroactively make an existing comment's mention ambiguous, changing how an old comment renders. Acceptable, but it means mention resolution is not stable over time.
- **`git stash` / `git stash pop` in this worktree left the index badly mangled** (every tracked file staged as deleted while present on disk); recovered with `git reset`. Avoid stash here — prefer a scratch commit or a second worktree when comparing against a baseline.

---

## Progress Update as of [2026-07-24 20:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update
First entry on this branch. Docs only — no application code changed. Added a brainstorm document exploring how to make it easier for Erika to see and respond to comments (she asked specifically about SMS delivery + SMS replies, and then noted comments could live in the admin view as well). The brainstorm is deliberately pre-spec: it surveys options across three axes and ends with a phased recommendation, but commits to nothing.

### Detail of changes made:
- Added `docs/superpowers/brainstorms/2026-07-24-comment-triage-brainstorm.md`. New `brainstorms/` subdirectory alongside the existing `specs/` and `plans/` dirs, for pre-spec ideation.
- The doc opens with an audit of the current comment system, which turned up three findings worth acting on regardless of which direction we pick:
  1. **There is no notification at all when an ordinary comment arrives.** `submitCommentAction` (`src/server/actions/comments.ts:134`) only fires email for @mentions. Loading the site is the only way to learn a comment exists.
  2. **`parseMentions` (`src/lib/comments/mentions.ts:8`) requires an exact full-display-name match**, so `@Erika` does not match a display name of `Erika Anderson`. The one notification path that exists is likely silently missing real mentions in production today.
  3. **`/admin/comments` (`src/app/admin/comments/page.tsx`) has no reply UI** — only Hide/Unhide on a flat 100-row list. Notably the *server* side already supports admin replies (`submitCommentAction` handles `parentCommentId` and an admin "post as" `actAsSignerId`), so this is a missing-UI problem, not a missing-capability problem.
- Doc structure: three axes — Delivery (SMS/Twilio, Telegram, WhatsApp, email, web push, Slack, ambient surfaces, voice), Triage (admin-view-as-inbox, importance scoring, triage state table, AI digest, cadence/quiet hours, mobile inbox, weekly rollup), Response (reply-by-text and its threading problem, AI-drafted replies, one-tap ack, voice reply, delegation, public expectation-setting).
- Section B0 (admin view as the canonical inbox) was added in response to Erika's mid-session note. Framed as the *starting point*, not an alternative: push channels are pointers back to the admin view, so building pokes before the inbox can act on them is backwards.
- Recommended phasing in the doc: Phase 0 = admin inbox + reply box + notify-on-every-comment + mention fix (hours, no vendors); Phase 1 = inbound email replies via Resend per-comment reply addresses + `comment_triage` table + importance score; Phase 2 = Twilio SMS for `needs-you` only; Phase 3 = AI-drafted replies + synthesized digest.
- Two constraints documented that affect sequencing: **no cron/scheduled-job infrastructure exists** (no `vercel.json`), which any digest needs; and **US A2P 10DLC registration** gates Twilio SMS by days of paperwork, which is why SMS is not the fastest path to a prototype and why Telegram is called out as the fast alternative.

### Potential concerns to address:
- The `parseMentions` exact-match bug is a live production issue, not a hypothetical. Small fix, should probably be pulled out of this brainstorm and shipped on its own.
- Reply-by-SMS posts publicly under Erika's name. The doc flags the safety rails as mandatory (undo window, confirmation echo, don't treat a phone number alone as authorization since caller ID is spoofable) — if this gets specced, those cannot be deferred to "later."
- AI-drafted or AI-sent replies on a site about AI ethics must be labeled as such. Called out in the doc; worth making an explicit spec requirement rather than an implementation detail.
- No notion of read/unread/handled exists in the schema, so there is currently no way to state truthfully how many comments are outstanding. Any triage feature needs this table first.
- Notification fatigue is the failure mode that invalidates the whole effort — if Erika mutes the channel, none of it matters. This is the argument for building the ranking before the texting, and it should survive into any spec.

---
