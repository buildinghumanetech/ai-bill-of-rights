# Branch Progress: sparkle/agent-e069631f-1128-4c77-9ca4-a13095f496f9

## Progress Update as of [2026-07-27 21:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Acted on the review of `28fd04f`. The nine pill remaps were all correct, so nothing here changes what the migration does to a database — it changes how hard it is to get the *next* one wrong. Statements 5 and 6 now carry their rename map once, as a joined `VALUES` list, instead of spelling the same set out twice per statement as `CASE` arms plus an `IN` filter; the redundant pill arms are gone from the move; and the coverage test now asserts the rename set in both directions, per statement, so a misspelled destination or a map that diverges between `comments` and `proposed_edits` fails. Also fixed `pnpm-workspace.yaml`, which was breaking every `pnpm` script in the repo including the one that applies migrations.

### Detail of changes made:

**`pnpm-workspace.yaml` — every `pnpm <script>` was failing, including `pnpm tsx scripts/apply-migration.ts`.** The file carried an `allowBuilds:` block whose four values were the literal string `set this to true or false` — pnpm's unanswered prompt, not a setting. pnpm cannot resolve that, raises `ERR_PNPM_IGNORED_BUILDS`, and exits non-zero from `runDepsStatusCheck`, which runs *before* the script does. Erika hit this trying to apply 0007 and 0008: both commands died in the preflight, never opened a DB connection. Answered all four with `false`, which is the state the lockfile has always installed under. Note `ignoredBuiltDependencies` alone does **not** settle it — pnpm re-added the placeholder block on the next run; the booleans have to be in `allowBuilds`. Verified `pnpm exec tsc --version` and `pnpm tsx --version` now exit 0 and the file is left untouched.

**The rename map is written once per statement now.** Statements 5-6 were `CASE WHEN … THEN …` over nine renames plus a nine-entry `anchor_id IN (…)` filter listing the same sources — two hand-synced copies per statement, where a rename added to the `CASE` but not the `IN` is a branch that silently matches nothing (the `WHERE` drops the row before the `CASE` is evaluated). Both are now `WITH "renames"("old","new") AS (VALUES …) UPDATE … FROM "renames" AS r WHERE "anchor_id" = r."old"`. One list, and the join *is* the filter.

**The move no longer duplicates the pill remaps.** A row the move carries forward lands on `0.1.0 AND is_current` still holding its old slug — exactly what statements 5-6 match on, with a superset of the move's rename set. The six pill `CASE` arms in the move were therefore dead weight and a fifth and sixth copy of a mapping that has been mis-transcribed twice. Removed; the move's `CASE` now carries only the `article-07-s-5` → `s-6` sentence shift. **The backup is unaffected** — `snap_comments` records the anchor *before* the move, so it still holds the original slug and the rollback still reverses the rename. Verified by the existing `records the pre-remap anchor so the move can be reversed` and `remaps renamed Connects-to pill anchors` tests, both untouched and still green.

**`covers every pill slug that changed` now asserts the set in both directions, per statement.** It previously only checked that each historical source literal appeared *somewhere* in the file, which cannot catch a misspelled destination (valid article number, source present, comment still orphans) and cannot catch a map that diverges between the two repair statements. New helper `anchorRemaps()` parses `(from, to)` pairs out of both forms the SQL uses (`WHEN … THEN` and `VALUES (old, new)`); the test asserts, for each repair statement independently, that the source set *equals* the renamed-and-not-superseded set and that every destination is in `livePillAnchors()`. Mutation-verified three ways, each failing the test: a typo in one destination, dropping the article-10 pair from the `comments` statement only, and adding a mapping for the deliberately-unremapped article-06 pill.

**Three more anchors documented as deliberately not remapped.** The previous entry treated `293640f` as *the* draft baseline. It is the third commit on this branch to touch `HomepageArticles.tsx`: `52690a7` and `447c41c` served `/proposed` with Article 10 on `humanebench-principle-dignity` and Article 11 on `humanebench-principle-honesty` / `humanebench-as-measurement-infrastructure`. Those pills were **swapped** for different principles, not renamed, so leaving a draft-tab comment on them stale is the correct call — but nothing said so, and the SQL comment's "UNION 293640f vs HEAD" read as a completeness claim about the draft population. Now named in case (d) of the migration header and asserted absent from the SQL via `DELIBERATELY_NOT_REMAPPED`.

**Test hygiene on the NULL-backup rollback test.** `await expect(rollback(db)).resolves.not.toThrow()` asserts nothing — vitest's `toThrow` short-circuits to a pass when the subject is a non-function under `resolves` + negation. Replaced with a plain `await` (a rejection still fails the test) and real assertions: the comment keeps its anchor *and* returns to v0.0.1, and the proposed edit's `target_anchor_id` survives.

**README.** The "how to write the next one" bullet said to derive the rename set from `main` vs HEAD — the exact rule that made 0008 incomplete. It now states the union rule, that *every* commit the preview served has to be checked rather than just the latest, and that only genuine renames get remapped. The rollback section's `COALESCE` note now says what it can and cannot do: it stops a NULL backup from orphaning the comment, but there is no original anchor to restore, so the row returns to v0.0.1 carrying its v0.1.0 slug.

### Verification

`./node_modules/.bin/vitest run` → **294 passed (41 files)**. `tsc --noEmit` clean. `eslint .` → **170 problems (161 errors, 9 warnings)**, exactly the baseline. Migration file alone: 19 passed, plus the three mutations above each failing as intended and the restored file passing.

### Potential concerns to address:

- **0008 still has to be applied by hand, after the deploy.** Nothing in CI or the Vercel build runs it, and it is a no-op unless v0.1.0 is *current* — so it must run after `sync-versions` has promoted the v0.1.0 row on postbuild. It is listed in README's "Post-deploy steps" along with 0007. Both are idempotent and safe to re-run.
- `HISTORICAL_PILL_ANCHORS` is a hand-maintained snapshot of three past commits. It cannot know about a *future* rename; whoever makes one has to add the old anchor to it. The set-equality assertion makes that failure loud (the test fails with the unmapped anchor named) rather than silent, which is the best this can do without reading git history from a test.
- The `anchorRemaps()` regexes are coupled to the SQL's formatting. Reformatting the `VALUES` list onto one line still parses; changing the quoting style would not. The set-equality assertion fails closed if the parse ever returns nothing, so a broken regex cannot silently pass.

---

## Progress Update as of [2026-07-27 08:45 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Two roborev jobs (99, 100) on `343918d` independently found the same High: **every anchor literal in migration 0008 used an unpadded article number, so no branch could ever match a row.** The app builds anchors from `article.number`, which has been the zero-padded string `"01"`…`"11"` since `HomepageArticles.tsx` was created — so the real anchors are `article-07-s-5` and `article-04-connect-…`, and the migration was looking for `article-7-s-5`. Case (c) and case (d) were both dead code against production. Fixed the SQL, fixed the root cause in the tests, added redirects for the six deleted resource URLs, and removed three unverified claims about HumaneBench's results.

### Detail of changes made:

**The defect, and why 288 tests passed over it.** The migration tests hand-typed their seed anchors in the same unpadded form the SQL used. Tests and SQL agreed with each other and both disagreed with the application. Nothing in the suite ever constructed an anchor the way the app does, so a migration that could not fire in production was fully green. This is the same self-pattern recorded on jobs 55/58/61/62 — fixing the case in front of me and generalising one step short — except here it was the *test* that was one step short.

- **`tests/lib/db.migration-0008-repoint-comments.test.ts`** now imports `articles` from `@/app/HomepageArticles` and derives every anchor through `sentenceAnchor(n, m)` / `pillAnchor(n, slug)`, which mirror `HomepageArticles.tsx:539` and `:604`. No hand-typed `"article-…"` literal remains in the file. `pillAnchor` takes the slug as an argument precisely because the interesting cases are *pre*-rename slugs that are no longer in the array — the padding comes from the app, only the slug is historical.
- **New test `uses only article numbers the app actually emits`** regex-scans the SQL for `'article-<n>-` literals and asserts every `<n>` is in `articles.map(a => a.number)`. This is the guard that catches the bug directly, without needing a seeded row. Verified load-bearing by mutation: reverting one literal to `article-7-s-5` fails 3 tests including this one.

**Rename set derived from `main`, not from the working tree.** Job 100 correctly flagged that `article-7-connect-humanebench-principle-dignity` was a branch for an anchor that never existed — Article 7 had no HumaneBench pill on `main`; its Protect Dignity pill is new in `293640f`. Diffing `git show main:src/app/HomepageArticles.tsx` against HEAD gives the true set: **five renames** (articles 01, 03, 04, 05, 09), plus `article-06-connect-…-empowerment` **removed** with no successor. Job 99's claim that articles 08 and 10 also needed remaps is wrong — neither pill existed on `main`, so there is nothing to carry forward. Deriving from `main` is what settled all three questions at once.

**Statements 5 and 6 (new).** The src→tgt move is scoped `WHERE base_version_id = src`, so it cannot reach rows already on 0.1.0: comments written on the /proposed tab before the rename, and any environment where an earlier form of 0008 already ran (it was revised several times on this branch — a stale dev application is likely). Two guarded `UPDATE`s repair those, matched on anchor value alone. Idempotent for free: the old slugs are gone from the app, so a row carrying one is orphaned by definition and a re-run matches nothing. Both still guard on `is_current`. Statement count assertion 4 → 6, with discriminating assertions on the new pair.

**`next.config.ts` — redirects for six deleted URLs.** The old resource slugs were live and linked from the homepage, so they are in browser history and in anything anyone shared; deleting the files 404s a URL that used to work. `RESOURCE_SLUG_REDIRECTS` is exported so tests can assert against it: every destination exists, no source shadows a live page (redirects are checked *before* the filesystem), and the real `redirects()` hook is exercised rather than the map it is built from. This also resolves the "unreferenced page" finding — `enhance-human-capabilities` is the redirect target for the old `empowerment` URL, which is why it stays.

**Removed three unverified claims.** `protect-dignity-and-safety` said HumaneBench evaluates "across 800 scenarios"; `prioritize-long-term-wellbeing` and `respect-user-attention` each stated specific first-round results. All three were mine, written from general knowledge, under a `sourceUrl` pointing at a page I could not read. Publishing unverified numbers about HumaneBench on Erika's own site is the wrong kind of risk, so the specifics are gone and the surrounding argument — which rests on the verbatim definitions I did read — stays.

**README.** The anchor-drift guidance now covers both classes (sentence counts *and* pill slugs), states the zero-padding rule explicitly, names the deliberately-unremapped pill, and warns that the rollback does not reverse statements 5–6 because those rows were never in the backup.

### Follow-up in the same commit: roborev job 111 on `eae8ccc`

The review of the fix found two more Mediums, both real, both verified before acting:

- **Statements 5-6 were missing four renames — the exact ones they exist for.** I derived their rename set from `main` vs HEAD, but those statements target rows *already* on 0.1.0, i.e. comments written against the v0.1.0 draft, which can be anchored to pills that only ever existed on this branch. Diffing `293640f` (the state the preview served while /proposed was live) against HEAD shows four such renames — articles 07, 08, 10, 11 — none of them in the `main` diff. Their set is now `main` vs HEAD **union** `293640f` vs HEAD. This is the same one-step-short pattern a third time: I correctly rejected job 99's 08/10 claim *for the src→tgt move* and did not re-ask the question for the statements where it was load-bearing.
- **The published rollback SQL would have orphaned comments.** An earlier form of 0008 had no `anchor_id` column, so in any database that ran it the backed-up anchor is `NULL`; the README's `SET "anchor_id" = b."anchor_id"` would write that NULL over a live anchor (leaving a comment with neither anchor nor proposal), and the second statement would then violate `NOT NULL` on `proposed_edits.target_anchor_id` — *after* the comments damage had committed as its own statement. Now `COALESCE(b."anchor_id", c."anchor_id")` wrapped in `BEGIN`/`COMMIT`, with a test that runs the rollback against a NULL-anchor backup. The ⚠️ paragraph I added in this same commit also asserted the opposite of what happens — it claimed those rows are not in the backup, when the earlier-run population is — and is corrected to distinguish the two groups.

Also: a new test `covers every pill slug that changed, from either starting point` asserts the rename SET, not just the number format — snapshotting the historical pill anchors from both starting points and requiring a branch for each, with `article-06-…-empowerment` explicitly asserted *absent*. Mutation-verified: deleting the article-10 branch fails it. This is the guard that would have caught the omission, and the previous scan could not — a missing branch uses perfectly valid article numbers. Plus a one-line note in the migration explaining why `next.config.ts` redirects the empowerment *page* while the *pill* anchor is deliberately not remapped (they look contradictory and are not), and a no-op `.sort()` on an expected array dropped.

### Verification

`./node_modules/.bin/vitest run` → **294 passed (41 files)**. `tsc --noEmit` clean. `eslint .` → **170 problems (161 errors, 9 warnings)** — exactly the baseline; a transient +1 was my own `.proof/pills.js` scratch file, since deleted.

### Potential concerns to address:

- **The publish is still blocked on Erika**, unchanged: `pnpm tsx scripts/unsync-version.ts 0.1.0 --allow-current --yes` then `pnpm sync-versions` against the dev Neon branch. The dev DB holds a frozen v0.1.0 whose hash no longer matches — and it has now been invalidated *again* by this commit's content edits. PR #31's Vercel check is currently FAILURE for this reason.
- **Open question for Erika:** Article 6 lost its HumaneBench pill entirely in her copy. `Enhance Human Capabilities` (the official rename of the old Empowerment page) therefore has a page but no pill. Should Article 6 link it again? If yes, the comments currently left dormant on `article-06-connect-…-empowerment` could be remapped to it in the same pass.
- **A test asserting pill *title* equals page title still does not exist.** `white-house-ai-bill-of-rights-2022` is linked from Articles 5, 10 and 11 under three different section-specific labels, and two EU AI Act pills likewise differ from their page titles. These look intentional, so the test would need an exemption list; not written rather than written wrong.
- The zero-padding trap is now guarded for migration 0008 specifically. **Any future migration touching anchors needs the same guard** — the test scans 0008's path only.

---

## Progress Update as of [2026-07-26 16:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Erika's correction: the `/resources/` pages must name the **actual HumaneBench principles**, using the benchmark's own terms and copy — not this repo's invented shorthand ("Dignity", "Honesty", "Non-Manipulation", "Empowerment"). Sourced the real eight from `github.com/buildinghumanetech/humanebench` and rewrote every HumaneBench page against them.

### Detail of changes made:

**Sourcing.** `humanebench.ai/principles` renders client-side, so WebFetch returns only the page title — it cannot be read this way, and I should not have implied a paraphrase was fine because of it. The definitions came from the HumaneBench repo instead, which states all eight verbatim:

1. Respect User Attention — "respect user attention as a finite, precious resource"
2. Enable Meaningful Choices — "empower users with meaningful choices and control"
3. Enhance Human Capabilities — "complement and enhance human capabilities, not replace or diminish them"
4. Protect Dignity and Safety — "protect human dignity, privacy, and safety"
5. Foster Healthy Relationships — "foster healthy relationships with devices, systems, and other people"
6. Prioritize Long-term Wellbeing — "prioritize long-term user wellbeing over short-term engagement metrics"
7. Be Transparent and Honest — "transparent about its operations and honest about its capabilities"
8. Design for Equity and Inclusion — "accessible and beneficial to diverse populations"

**Pages rewritten and renamed.** Each page now leads with the official definition as its subtitle and works from it. Slugs follow the principle names, so the URL and the page agree:

- `humanebench-principle-dignity` → `…-protect-dignity-and-safety`
- `humanebench-principle-non-manipulation` → `…-enable-meaningful-choices`
- `humanebench-principle-empowerment` → `…-enhance-human-capabilities`
- `humanebench-respect-user-attention` → `humanebench-principle-respect-user-attention`
- `humanebench-principle-honesty` **and** `humanebench-principle-transparency` → a single `…-be-transparent-and-honest` (they were one principle split across two pages; Articles 3 and 5 now share it)
- the two pages I created last commit renamed to the official forms

`Foster Healthy Relationships` has no page — no article references it. Left uncreated rather than adding unreferenced content.

**Pill slugs are comment anchors, so this needed a migration.** `HomepageArticles` builds `article-N-connect-<slug>`, and comments can be anchored to a pill. Renaming a slug therefore orphans any comment on it — the same class of silent failure as the sentence anchors. 0008 gained case (d): six remap branches, one per rename.

Deliberately NOT remapped: `article-6-connect-humanebench-principle-empowerment` and `article-10-connect-humanebench-principle-dignity`, where Erika **removed or swapped** the pill rather than renaming it. Reattaching those comments to an unrelated principle would misrepresent what someone wrote. They stay put and are recoverable from the backup. A test asserts both halves — the rename moves, the removal does not.

**Two new standing tests** (`bill-of-rights.content.test.ts`):

- every "Connects to" pill has a resource page behind it. The pill list is a hand-maintained array in a different file from the content it points at, so a typo was a 404 nothing else caught — this is how I found the four missing pages in the first place, by an ad-hoc script rather than the suite.
- every `humanebench-principle-*` slug is one of the eight official principles. A pill that invents a name for one misattributes the benchmark.

### Verification

`288 tests (41 files) passed` (up from 285), `tsc --noEmit` clean, eslint at the 170-problem baseline.

### Potential concerns to address:

- **The page bodies are still my prose**, now built on the official one-line definitions rather than on my guess at what each principle meant. The definitions and names are verbatim; the surrounding paragraphs are not. If HumaneBench publishes longer official copy, it should replace mine.
- The definitions came from the HumaneBench **repo**, not `humanebench.ai/principles`. Same organisation, and the eight names match the press coverage of the v1 results, but the site remains the canonical source and could not be read.
- `humanebench-as-measurement-infrastructure` is not a principle page and keeps its slug.
- All prior concerns stand.

---

## Progress Update as of [2026-07-26 15:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Erika reviewed the applied copy and reverted two of her own changes: Article 2 keeps "Memory built on your life is yours." (not "LLM memory…") and Article 6 keeps "The loop stays open." (not the AI-agent "license plate" sentence). Both articles are byte-identical to v0.0.1 again.

### Detail of changes made:

- `content/bill-of-rights/v0.1.0.md` — article-2-s-4 and article-6-s-3 restored to their v0.0.1 text.
- `src/app/HomepageArticles.tsx` — the two pull quotes follow, or the homepage would show wording absent from the document, which is the exact gap this publish closed.
- `tests/lib/bill-of-rights.content.test.ts` — `article-2` and `article-6` removed from `DECLARED_REVISIONS`. Leaving them would exempt two articles from drift detection for a revision that no longer exists; the companion honesty test fails on precisely that, and did.
- `drizzle/0008_…sql` — case (b) in the header now lists only article-1-s-4 and article-4-s-1, both of which keep their claim and only sharpen it. **The revert removes the one genuinely awkward case**: article-6-s-3 had been the only sentence that changed SUBJECT under existing comments, and the header no longer has to justify moving those threads.
- Changelog (frontmatter + `versions.json`) and `v0.1.0.agents.md` now say Articles 1, 4, 5 and 7 rather than 1-9.

Unchanged by this revert: Article 7 still inserts the COPPA definition mid-article, so the `article-7-s-5` → `article-7-s-6` remap is still required and still tested.

### Verification

`285 tests (41 files) passed`, `tsc --noEmit` clean, eslint at the 170-problem baseline. The pull-quote match test passing with an empty divergence set is what confirms the homepage and the document agree on both reverted articles.

### Potential concerns to address:

- **I committed the revert without updating this log first, and amended after.** The workflow in CLAUDE.md is not optional; the pre-commit hook warns but does not block, which is exactly why it needs deliberate attention rather than reliance on the backstop.
- Two HumaneBench pill labels still need Erika's ruling: "Enable Meaningful Choices" currently points at the `humanebench-principle-non-manipulation` page (whose *content* matches Article 4) rather than `humanebench-principle-empowerment` (whose *name* is closer). Empowerment is now orphaned, since she removed it from Article 6.
- `humanebench.ai/principles` is client-rendered, so it could not be fetched to source the official principle text. The two new principle pages remain my prose, in the same inferred-summary style as the six that already existed in this repo.
- All prior concerns stand.

---

## Progress Update as of [2026-07-26 14:15 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

**Erika sent the final copy for all 11 Articles**, which unblocks the publish that had been on hold since the 25th. Applied it across the canonical document, the homepage, the agents guide, the spec, the resource pages and the 0008 migration. The headline outcome beyond the wording itself: her copy **closes the pull-quote divergence entirely** — `KNOWN_PULLQUOTE_DIVERGENCES` is now empty, where it had four entries.

### Detail of changes made:

**The document (`content/bill-of-rights/v0.1.0.md`)**

Articles 1, 2, 4, 5, 6, 7 revised; 3, 8, 9 unchanged; 10 replaced with Erika's wording (it had been my draft — the thing she said she still could not find); 11 rewritten and retitled to "A Right to Safe, Tested Systems".

The structural change is that **every article now closes with its pull quote**. Previously the homepage rendered pull quotes that were not in the canonical document at all — Article 6's showed an AI-agent "license plate" sentence appearing NOWHERE in the text people were signing, and 01/02/05 showed paraphrases. Those four had been carried as a listed, shrinkable exemption precisely so they could not be forgotten. Erika's copy adopts the homepage wording into the document, so the exemption list is now empty and the test that keeps it honest will fail if anyone reopens the gap.

**Anchor consequences — the part that needed real care**

Sentence counts changed, and comments anchor to `article-N-s-M`. Three cases:

- *Appended* (Article 5 gains two sentences, 11 is new): existing anchors unaffected.
- *Changed in place* (article-1-s-4, 2-s-4, 4-s-1, 6-s-3): the anchor still resolves to the same slot. For 1, 2 and 4 the claim is the same and only sharper. **article-6-s-3 genuinely changed subject** — "The loop stays open." became the license-plate sentence. There is no better anchor to move those comments to and dropping them would lose real discussion, so they move. Highlights degrade safely because `applyHighlights` substring-matches `selected_text`, so a stale selection renders unhighlighted rather than landing on the wrong words.
- *Inserted mid-article* — exactly one, and the dangerous one. Article 7 gains the COPPA definition as the new s-5, pushing "Children's data is not a training asset." to s-6. Left alone, every comment on `article-7-s-5` would have silently re-attached to a definition nobody wrote about. **The anchor still resolves, which is exactly why nothing would have complained.** 0008 now remaps `article-7-s-5` → `article-7-s-6` for the rows it moves.

**0008 re-scoped (`drizzle/0008_repoint_comments_to_v0_1_0.sql`)**

Its correctness argument was "Articles 1-9 are byte-identical, so every anchor still resolves". That is dead, and the header now says so and enumerates the three cases above. Backup tables gained an `anchor_id` column (with an idempotent `ADD COLUMN IF NOT EXISTS` for leftovers from the earlier form) so the remap reverses along with the move. The remap is idempotent for free: it only matches rows still scoped to 0.0.1, which the same statement moves to 0.1.0, so a re-run cannot walk s-6 on to s-7 — and there is a test that would catch it if that coupling ever broke.

**Connects / resources**

Erika's reference lists replace the old ones. HumaneBench principle pills were retitled to her names (Protect Dignity and Safety, Be Transparent and Honest, Enable Meaningful Choices, Respect User Attention) keeping their existing slugs, so no resource page URL breaks. Four references had no page and now do: `eeoc-guidance-ai-employment`, `eu-ai-act-article-12-record-keeping`, `humanebench-principle-long-term-wellbeing`, `humanebench-principle-equity-inclusion`. A check confirms every one of the 34 pill slugs resolves to a file.

Note: `/resources/[slug]` has **no markdown renderer** — every field is a plain text node — so the first draft of those pages rendered `**bold**` and `*italics*` as literal asterisks. The existing test caught it. Rewritten as plain prose.

**The carry-forward guard is now declarative, not deleted (`tests/lib/bill-of-rights.content.test.ts`)**

"A new version may only ADD articles, never reword one" would have failed on this change, and simply deleting it would remove the only protection against a stray unreviewed edit to a carried-forward article — text signers believe they already agreed to. Replaced with `DECLARED_REVISIONS`, a per-version set of articles whose revision was intentional. A version with no entry must still carry everything over verbatim. A companion test fails if a declaration outlives the revision it described, so the list cannot silently exempt an article forever.

### Verification

`285 tests (41 files) passed`, `tsc --noEmit` clean, eslint at the 170-problem baseline. The remap test was verified load-bearing by neutering the `CASE`: `article-7-s-6` → `article-7-s-5`. Three new migration tests cover the remap, the re-run, and that the backup stores the pre-remap anchor.

### Potential concerns to address:

- **The four new resource pages are my prose, not Erika's.** They summarize public regulatory and HumaneBench material and I believe them accurate, but they are the one part of this change she has not written or reviewed. Worth her eye before the publish goes out.
- Two HumaneBench pills now share the label "Be Transparent and Honest" while pointing at different pages (`humanebench-principle-honesty` on Article 3, `humanebench-principle-transparency` on Article 5). That follows her lists exactly; merging them would orphan a page, so it is left as a question for her.
- Three resource pages are now unreferenced by any article (`humanebench-principle-empowerment`, `us-civil-rights-anti-discrimination-law`, `model-cards-and-system-documentation`). Files left in place — still reachable by URL, nothing deleted.
- **The dev Neon branch still holds a frozen v0.1.0 row whose hash no longer matches this text.** The unsync + re-sync is still required, and now doubly so.
- Re-affirm still sends no confirmation email — Erika's call.

---

## Progress Update as of [2026-07-26 13:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed roborev job 62 — five findings on the restorability guard I added the commit before. The Medium one is that the guard **answered the wrong question**: `versionRestorability` checked whether the row would come back, when what it is protecting is whether the *current* row would come back. Those differ, and the gap was reachable.

### Detail of changes made:

**The guard now asks about the CURRENT row (`src/lib/content/versions-index.ts`)**

`sync-versions` derives `is_current` solely from `entry.version === index.current`. Two holes followed from ignoring that:

- If `versions.json` names a `current` that is in no history entry — a typo, or `current` bumped before its history entry was added — `syncVersions` takes its `else` branch, clears `is_current` on every listed version, and leaves **no current row**. Deleting the DB's current 0.1.0 passed the old guard cleanly (0.1.0 in history, all three files present) and the re-sync then produced exactly the permanent state the guard exists to prevent. `readVersionsIndex` now rejects such an index outright — it is the validating reader, and an index that would produce a currentless database is not well-formed.
- If `current` names a *different* history entry, the delete was honoured and the re-sync silently moved `current` to that other version. `versionRestorability` now refuses when `version !== index.current`, and the reason names what `sync-versions` would actually make current.

**`isRestorable` is required whenever `allowCurrent` is set (`src/lib/db/unsync-version.ts`)**

It was optional against `if (row.isCurrent && opts.isRestorable)`, so a caller that omitted it got no check and a successful delete — the same omission-falls-through-to-permissive failure `UnsyncReport` had just been made a discriminated union to prevent. `UnsyncOpts` is now a union: `allowCurrent: true` requires the predicate. Adding it immediately produced three compile errors in the test file — three tests that had been deleting a current row with no predicate at all, which is precisely how the permissive default looked normal.

**Two smaller ones**

- `readVersionsIndex` validated that `history` was an array but not what was in it, and the membership scan ran *outside* `versionRestorability`'s try/catch — so `[null]` threw `Cannot read properties of null` past both and landed in the CLI as a raw stack, the exact failure the module's docstring claims to prevent. Each entry is now checked, naming the bad index.
- The `readFileSync` catch discarded the error and reported everything as "does the file exist?", sending an operator hunting for a file that is sitting right there when the cause is EACCES or EISDIR. It now carries the errno. Also fixed the refusal string reading `does the file exist?. Deleting…` — joined with an em dash instead of `.`.

**Test for the ordering (`tests/lib/db.unsync-version.test.ts`)**

`not_restorable` must be checked *before* `dry_run`, because the dry run is what an operator hits first and the CLI prints "safe to delete, re-run with --yes" for the `dry_run` code. Every other new test passed `dryRun: false`, so moving the dryRun block up would have made the tool actively recommend an unrecoverable delete with nothing failing. Now covered.

### Verification

`281 tests (41 files) passed`, `tsc --noEmit` clean, eslint at the 170-problem baseline. The standing "this repo's own current version is restorable" check still passes, which now additionally confirms `versions.json`'s `current` is in history and matches what we would unsync.

### Potential concerns to address:

- **Third consecutive job where the finding was in code I had just written to fix the previous job's finding.** The pattern is consistent: I fix the case in front of me and generalise one step short. Worth building in an explicit "what else does this predicate/type/guard need to be true of" pass before committing a fix.
- All prior concerns stand, minus the wording block — **Erika sent the final copy for all 11 articles**, which is the next piece of work.

---

## Progress Update as of [2026-07-26 12:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed roborev job 61 — six findings on the re-affirm commit. The headline one is **a regression I introduced two commits ago**: gating `signed-other` on `is_current` collapsed "this version is archived" together with "this version has no row at all", so an existing signer hitting the site during any window where the deployed version constant is ahead of the database would be told v0.1.0 "is no longer open for signing" — a false statement about the version the site is actively campaigning for, with no sign path offered.

### Detail of changes made:

**`signed-version-unknown` (`src/lib/db/signature-status.ts`)**

`sync-versions` is a manual post-deploy step and `SignModal` hardcodes `VERSION = "0.1.0"`, so "no row for the requested version" is a state this branch can actually produce — the unsync/re-sync remedy for the frozen draft creates that window on purpose. It is now its own state. The modal branch for it says **nothing at all** about the requested version — just "your signature stands and still appears in the public list" — because that is the only claim that holds whatever the cause. `resolveSignatureStatus` `console.warn`s the missing row, so the operational fault surfaces where operators look rather than as copy shown to a signer.

Worth noting what the old code did here: the pre-`is_current` predicate fell through to `signed-earlier`, so the re-affirm button was at least offered and would have failed loudly. My change made it fail quietly and wrongly instead. Quiet-and-wrong is the worse failure.

**The Remove button's blast radius, stated in the copy (`SignModal.tsx`)**

I had given `signed-other` its own branch partly because the `signed` branch offers "Remove my signature", which hard-deletes the signer row and **every** signature. That hazard was unchanged for the sibling case — someone viewing `/v/0.0.1` who signed 0.0.1 still resolves to `signed`, same wrong reading, same global delete — and only the hardcoded constant kept it out of reach, which is exactly the reachability argument I had rejected for `signed-other`. The confirm copy now says it removes your name from *every version* you have signed, not just the one you are viewing. Fixing it in the wording fixes it for every branch that keeps the button, rather than only the one I happened to look at.

**`RateLimitError` (`src/lib/ratelimit/enforce.ts`)**

`me.ts` was matching `/rate limit exceeded/i` against a message produced in another module — the identical free-text-sentinel-across-a-module-boundary contract that `refusedCode` was introduced to kill in `unsync-version` **one commit earlier**. `enforceRateLimit` now throws a typed `RateLimitError` carrying `code`, `bucket`, `count`, `max`, `windowSec`. The message is unchanged, because the two other callers surface it to the user directly.

**Removed the reaffirm rate limit rather than pretend (`src/server/actions/me.ts`)**

It could not trip. `reaffirmSignature` writes at most one signature per (signer, version) — unique index — and writes a consent record only when it is about to create that signature, deleting it again on failure. So a `count(*) FROM signatures WHERE signer_id = $1` bound sits at 1 forever regardless of how often the action is called. Hitting `max: 5` would need five versions published *and* signed inside an hour. A limit that can never reject is worse than none, because it reads as protection. Removed, with a comment explaining precisely why the writes are already bounded and what a real limiter would need.

**What is genuinely not fixed:** bounding *attempts*. A rejected caller can still loop the refusal paths for free (auth round trip, two or three selects, a consent render, a sha256 — all CPU and cheap reads, no writes). Counting attempts needs somewhere to record them, i.e. a rate-limit table and a hand-applied migration, which is Erika's call rather than something to smuggle in as a review fix. The comment in `me.ts` says so.

**Docstring order (`reaffirm.ts`)** — the header claimed the guards were listed "in the order they fire" and that the order was load-bearing, then listed them in the wrong order. A maintainer trusting it would have concluded a never-signed caller always gets "Sign the Bill of Rights first"; in fact a commenter probing an archived version gets "no longer open for signing". Renumbered to match the code: version exists → is current → has signed something → already signed this one.

### Verification

`277 tests (41 files) passed`, `tsc --noEmit` clean, eslint at the 170-problem baseline. New tests: the no-row-yet state, a non-duplicate insert failure (asserting the consent record is deleted **and** the error rethrown — the branch the `catch` was widened for had no coverage, so moving the delete back inside `if (/duplicate key/)` would have passed everything), and `RateLimitError`'s type, code and preserved message. Reverting the row-absent branch reproduces the finding exactly: `expected 'signed-other' to be 'signed-version-unknown'`.

### Potential concerns to address:

- **Second commit running where I reintroduced a pattern I had just fixed elsewhere** (free-text sentinel across a module boundary). The earlier note about repeat findings generalises: a fix applied in one module is worth grepping for in the others before the next commit.
- Attempt-rate-limiting is deliberately unaddressed pending a decision on adding a rate-limit table + migration.
- If `SignModal`'s hardcoded `VERSION` and the database disagree, someone who has **never** signed still gets the sign form and their submit fails at `recordSignature`. Pre-existing, out of scope of this job, but the same root cause as finding 1.
- All prior concerns stand: publish on hold for Erika's wording of Articles 1–9; 0008 needs re-scoping if any change; sentence-count drift re-points comment anchors; Article 10 is still my draft; re-affirm sends no confirmation email; the Vercel check needs the `--allow-current` unsync + re-sync against dev.

---

## Progress Update as of [2026-07-26 11:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed roborev job 58 — five findings, all on the `unsync-version` tooling, and **four of them were consequences of one mistake**: I put the new restorability guard in the CLI, gated on the `--allow-current` *flag*, running before the database was consulted. Moving it into the lib and keying it on the row's real `is_current` fixed the false refusals, the wrong error message, the untestability, and the duplicated `versions.json` parsing at once.

### Detail of changes made:

**New `src/lib/content/versions-index.ts`**

One place that knows how to read `versions.json` and what it takes to restore a version. Exports `CONTENT_ROOT`, `readVersionsIndex()` (validates shape; throws naming the path and the actual problem rather than letting a raw ENOENT or `… is not iterable` surface three frames deeper), `versionFileNames()`, and `versionRestorability()`. Both scripts use it; `sync-versions.ts` lost its private `VersionsJson` interface and inline `JSON.parse`.

**The guard moved into `unsyncVersion`, keyed on `is_current`, not the flag**

`unsyncVersion` now takes an injectable `isRestorable?: (version) => Restorability` — injected so the lib stays free of `fs` and the predicate is testable on its own. The check runs *after* the not-found and dependents checks and only when `row.isCurrent`. Three bugs fell out of the old placement:

- A **non-current stale leftover absent from disk** — the exact thing this tool exists to clean up — became undeletable for anyone who passed the flag, which the README tells operators they will almost always need.
- A **typo'd version** plus the flag exited 1 with "the database would be left with no current version", a claim about a row that does not exist, instead of the previous `No row for version X — nothing to do.` at exit 0.
- The refusal text asserted something false about non-current rows.

**Restorability now means all three files, not just history membership**

`sync-versions` reads `v<x>.md`, `v<x>.agents.md` and `v<x>.spec.json` per entry. A history entry whose markdown or spec was renamed passed the old check, the delete succeeded, and the follow-up `sync-versions` threw ENOENT before inserting anything — producing exactly the permanent no-current-version state the guard was added to prevent, while the message claimed that had been ruled out. The reason string names the missing file.

**`UnsyncReport` is a discriminated union**

`refusedCode` was optional and independent of `deleted`, so a future refusal path setting only `refusedBecause` would type-check, reach the CLI's fall-through, and print `Refused: <text>` at exit 1 — the silent control-flow failure `refusedCode` was introduced to eliminate. It is now `{ deleted: true }` vs `{ deleted: false; refusedCode; refusedBecause }`, so omitting the code is a compile error. This immediately caught 13 call sites in the test file, which now go through a `refusal()` narrowing helper that fails with "expected 0.1.0 to be refused, but it was deleted" instead of comparing against `undefined`.

**The migration test's sentinel (finding 4 — my comment was wrong, not the code)**

The rewritten seeding was discriminating, but I had justified it with `currentId` being "a value only a pre-existing snapshot could hold". It is not — it is the *post-move* mapping, i.e. precisely the overwritten state the original assertion was written to catch. So the test asserted the backup keeps pointing at the **new** version while the surrounding prose talked about preserving the original mapping "so rollback still works"; a maintainer reconciling those would have changed the wrong side. Now an unrelated sentinel uuid belonging to no version row, with the property restated as what it actually is: the pre-existing row is left untouched, whatever it holds. The pre-update-mapping property was already covered separately.

### Verification

`274 tests (41 files) passed` (up from 259), `tsc --noEmit` clean, eslint at the 170-problem baseline. New `tests/lib/content.versions-index.test.ts` covers the reader and the predicate, including a `it.each` over all three files and a standing check that **this repo's own current version is restorable** — if that fails, the README's frozen-draft remedy has no working path. Load-bearing checks:

- gate disabled → `expected 0.1.0 to be refused, but it was deleted`
- gate re-keyed to `opts.allowCurrent` (the shipped bug) → the non-current-leftover test fails, reproducing the finding exactly
- `ON CONFLICT DO NOTHING` → `DO UPDATE` still yields `expected +0 to be 1` under the new sentinel

Also smoke-ran the CLI: it reaches `unsyncVersion` before failing on the absent `DATABASE_URL`, confirming the static `@/lib/content/versions-index` import resolves and that no filesystem check now precedes the DB read.

### Potential concerns to address:

- `versionRestorability` reports an unreadable `versions.json` as *not restorable* rather than throwing. Deliberate — the caller is deciding whether a delete is safe, and "I could not tell" must not read as "yes" — but it means a corrupt index shows up as a refusal about restorability rather than about the index.
- All prior concerns stand: publish on hold for Erika's wording of Articles 1–9; 0008 needs re-scoping if any of them change; sentence-count drift silently re-points comment anchors; Article 10 is still my draft; re-affirm sends no confirmation email (Erika's call); the Vercel check still needs the `--allow-current` unsync + re-sync against dev.

---

## Progress Update as of [2026-07-26 10:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed roborev job 55 — six findings on the re-affirm path, **one of which was an authorization hole I introduced myself**: `reaffirmSignature` never checked that the caller had signed anything, so any authenticated commenter could have made themselves a public signatory of the bill without ever passing the consent gate. Fixed, tested, and the test verified load-bearing by deleting the guard and watching it fail.

### Detail of changes made:

**The hole (`src/lib/db/reaffirm.ts`)**

`createSignerFromModal` gives comment-only accounts a `signers` row with **zero signatures**. `reaffirmSignature` only ever checked "have you signed *this* version" — so for a commenter the answer was no, and it happily wrote a real signature row. That routes around `submitSignAction`'s `consent === "yes"` gate and the consent page entirely. The fix is one read serving both needs: select the signer's signatures once, refuse on `own.length === 0` ("Sign the Bill of Rights first — there is nothing to re-affirm"), short-circuit on `own.some(s => s.versionId === versionRow.id)`. The three guards are now documented in the function header in the order they fire, because the order is load-bearing.

**Orphan consent records on every error path, not just the race**

The consent record is written before the signature and neon-http has no transactions, so nothing rolls it back. The cleanup lived only in the duplicate-key branch; any other insert failure leaked. The `db.delete` now runs in the `catch` before the error is classified, best-effort (a failed delete must not mask the real error). `consent_records` has no unique constraint, so a leak here is unbounded, not one row.

**`signed-newer` was the wrong question (`src/lib/db/signature-status.ts`)**

Renamed to `signed-other` and re-gated on the requested version's `is_current` rather than a publish-date comparison. The gap the old name hid: an **archived intermediate** version (signed 0.0.1, viewing a published-then-archived 0.0.2) is newer *and* unsignable, so it returned `signed-earlier` and rendered an "Add my name to v0.0.2" button that `reaffirmSignature` could only ever refuse. The predicate that matters is "is this version open for signing", which is exactly `is_current` — the same thing `reaffirmSignature` checks, so the two can no longer disagree.

**`signed-other` got its own branch in `SignModal.tsx`**

It had been folded into the `signed` view, which says "you've already signed *this*" (untrue — it shows the version they signed, not the one they're viewing) and offers **Remove my signature**. That button calls `removeMySignature`, which hard-deletes the signer row and *every* signature they own. On an archive page a reasonable person reads it as removing that one. The new branch has no remove button and links to `/account` instead.

**The rate limit was counting the wrong table (`src/server/actions/me.ts`)**

It counted `consent_records`, which `reaffirmSignature` short-circuits before writing — and since the unique index permits at most one signature (hence one consent record) per (signer, version), that counter sat at 1 no matter how often the action was called. It now counts `signatures`, the thing this action actually writes. Also split the `catch`: only a genuine "Rate limit exceeded" reports as "too many attempts"; a DB/SQL failure logs and returns the generic error, instead of sending someone away to retry something that is not their fault and will not fix itself.

**Tests (`tests/lib/db.signature-status.test.ts`)**

New `seedPriorSigner` helper — re-affirming now *requires* a prior signature, so every re-affirm test needs one. Three assertions were rewritten to account for it rather than papered over:

- the hash-stamp test now selects the 0.1.0 signature by `(signerId, versionId)` and additionally asserts the stamp is **not** the 0.0.1 hash;
- the repeat-no-op test asserts 2 signatures (the one they arrived with + the one created), not 1;
- the not-current test now seeds the signer on **0.1.0** and asks about **0.0.1** — deliberately that way round, because with the old seeding the already-signed short-circuit would have caught it and the write assertions would have passed with the guard deleted.

### Verification

`259 tests (40 files) passed`, `tsc --noEmit` clean, eslint back to the 170-problem baseline (the `and` import in `reaffirm.ts` went unused when the two reads collapsed into one). Every guard verified load-bearing by injecting its own regression:

- `own.length === 0` → `expected true to be false` on the commenter test
- consent-record `delete` → `expected 2 to be 1` on the orphan test
- `Boolean(requested[0].isCurrent)` → both `signed-other` tests fail

### Potential concerns to address:

- **Job 58 (on `df51759`) is open with 5 findings, all against the `unsync-version` CLI** — the headline one is that the new history guard fires on the `--allow-current` *flag* before the DB is read, so a non-current stale leftover becomes undeletable and a typo now exits 1 with a false message instead of exiting 0. Being fixed next.
- The re-affirm path still sends **no confirmation email**. Deliberate and documented — a routine version publish can trigger it for the whole signer list — but it is Erika's call, not mine.
- All prior concerns stand: publish on hold for Erika's wording of Articles 1–9; 0008 needs re-scoping if any of them change; sentence-count drift silently re-points comment anchors; Article 10 is still my draft; the Vercel check needs the `--allow-current` unsync + re-sync.

---

## Progress Update as of [2026-07-25 11:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed roborev job 47 — six findings, **two of which were repeats I had let slide across three commits**. Worth recording as a pattern: a finding that is not fixed the round it is raised does not go away, and one of these (`dryRun` defaulting to delete) got materially more dangerous *because* of the fix I shipped in between.

### Detail of changes made:

**`dryRun` defaulted to DELETE (repeat, raised on b29dc89)**

`opts.dryRun` was falsy-by-default while the file header and the CLI both advertised "dry run by default". Adding `allowCurrent` widened the blast radius: `unsyncVersion(db, v, { allowCurrent: true })` — the obvious way to write the new call — deleted the **current** version row with no dry-run step. Now `const dryRun = opts.dryRun ?? true`. The CLI passes `dryRun: !confirmed` on every path, so nothing else changed. Two new tests cover the bare call and the `allowCurrent`-only shape.

**Free-text sentinel (repeat, raised on b29dc89)**

The CLI branched on `report.refusedBecause === "dry run"` — a display string produced in another module. The previous commit reworded a *neighbouring* refusal, which is exactly the edit that breaks this: a reworded sentinel silently turns a successful dry run into `Refused: <text>` with exit 1. Added `refusedCode: "not_found" | "referenced" | "current" | "dry_run"` to `UnsyncReport`; `refusedBecause` is now display-only and documented as such. A test pins all four codes.

**A test assertion of mine that could not fail**

I had asserted the 0008 backup "preserved the ORIGINAL mapping" by seeding the leftover from `WHERE base_version_id = oldId` — but the migration's own `snap_` CTEs insert *pre-update* values, i.e. also `oldId`. So `DO NOTHING`, `DO UPDATE`, and no conflict clause at all left the row identical and the check passed regardless. Now seeded with a **distinguishable** mapping (`currentId`, a value only a pre-existing snapshot could hold). Also added a real keyless precondition via `pg_constraint contype = 'p'` — previously only row counts were asserted, so a Postgres that carried a key through CTAS would have made the test silently stop exercising the `DO $$` repair.

**Three smaller ones**

- The dry run — the step whose entire job is to inform the decision — never mentioned that `--allow-current` leaves the database with no current version, because that warning printed only *after* the delete, and the refusal carrying it is the one you stop seeing once you pass the flag. It now prints in the dry-run branch.
- `--allow-current` now refuses a version absent from `versions.json` history: `sync-versions` seeds from that history, so a DB-only version would be deleted **permanently**, leaving no current version — not the temporary state the README describes.
- `expect(required.length).toBeGreaterThan(0)` in the agents.md guard turned a legitimate state (a patch release adding no articles, or a first-ever version) into a failure, and disagreed with the sibling carried-forward test. Now returns early, matching it.

### Verification

256 tests (40 files), `tsc --noEmit` clean, eslint at the 170-problem baseline. The rewritten migration assertion was verified discriminating by flipping `ON CONFLICT DO NOTHING` to `DO UPDATE`: `expected +0 to be 1`. Under the old seeding that flip passed.

### Potential concerns to address:

- **Pattern worth watching: two of these six findings were repeats.** Check `roborev show` output for "raised on <sha>, still open" before closing a job.
- All prior concerns stand: publish on hold for Erika's wording of Articles 1–9, 0008 needs re-scoping if any of them change, sentence-count drift would silently re-point comment anchors, Article 10 still my draft, re-affirm sends no confirmation email (Erika's call), Vercel check needs the `--allow-current` unsync + re-sync.

---

## Progress Update as of [2026-07-25 11:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed roborev job 43 — seven findings on the signature carry-forward work, all valid. The most serious: `reaffirmMySignature` was an unvalidated one-argument server action that let any authenticated user attach signatures to any version row and leaked an orphan `consent_records` row on every repeat call. It also had **no test at all** — the test I titled "…when the person re-affirms" never called it.

### Detail of changes made:

**Hardening the write path**

- **`src/lib/db/reaffirm.ts` (new)** — extracted the body of `reaffirmMySignature` into an injectable `reaffirmSignature(db, input)`, matching `resolveSignatureStatus` and `deleteSigner`. It was previously untestable: unlike `recordSignature(dbClient, input)` it took no db parameter and resolved the client through module-level `getDb()`. Three guards it now has:
  1. **Only the current version may be affirmed.** The client supplies the version string, so without this a signer could attach rows to any archived version — something no surface offers.
  2. **An existing signature short-circuits to `{ ok: true, created: false }`.** `recordSignature` inserts the consent record *before* the signature, so detecting a repeat via the unique-constraint violation leaked a `consent_records` row every time. `consent_records` has no unique constraint, so a loop wrote unbounded rows.
  3. The duplicate-key catch is kept, but now only for a genuine concurrent race.
- **`src/server/actions/me.ts`** — wired in `enforceRateLimit` (10/hour per signer, counting `consent_records`), which was previously only on the comment actions.

**Correctness and honesty fixes**

- **`signature-status.ts`** — `firstSignedAt` was paired with the *latest* version in the modal's "signing since" line. For anyone with two prior signatures that states a date they did not sign that version on. Added `firstVersion` so the pair travels together, and the modal now shows the most-recent signature separately when they differ.
- **New `signed-newer` state** — `signed-earlier` was returned for *any* non-matching version, including one **older** than what the person signed (viewing `/v/0.0.1` after signing 0.1.0). Compared by `publishedAt`, not by parsing version strings, which would get `0.10.0` vs `0.9.0` wrong. The modal renders it as the plain already-signed view: there is nothing to re-affirm on a superseded version.
- **Corrected a comment I had written that was simply false.** It claimed the re-affirm hashes consent "against the text of the version being affirmed". It does not — `renderConsentText` renders `content/consent/v*.md`, the data-collection disclosure, which contains no bill text, so that hash is identical whichever version is affirmed. What actually binds the signature to the new text is `signatures.version_hash_at_signing`. Since the whole ethical argument for one-click re-affirm rested on that sentence, leaving it would have had a future maintainer over-trusting the consent record. Both the action and modal comments now point at the right field, and a test asserts the stamp.
- **`SignModal`** — removed the hardcoded "added two new Articles" sentence naming Article 10 and 11. It was a second, unguarded transcription of the document text (already drifting: "the Right to Safe, Tested Systems" vs the canonical "**You Have** the Right to Safe, Tested Systems"), it sat outside the drift guards, and it would be wrong for any other version pair. Especially wrong to keep with Erika about to reword everything.
- **Documented that no confirmation email is sent** on this path, unlike the first-time sign paths, and why: a routine version publish makes this action available to every existing signer, and mailing the whole signer list should be a decision made on purpose. Flagged for Erika.

**Tests (6 new, 12 in the file)** — re-affirm end to end, the version-hash stamp, three repeat calls leaving exactly one signature and no extra consent record, refusal of a non-current version writing *nothing*, refusal of an unknown version, the `firstVersion` pairing, and `signed-newer`.

### Verification

254 tests (40 files), `tsc --noEmit` clean, eslint at the 170-problem baseline. The `firstVersion` guard verified against exactly the swap roborev said would leave the suite green: `expected '0.0.2' to be '0.0.1'`.

### Potential concerns to address:

- **Should re-affirm send a confirmation email?** Currently silent and documented as such. Erika's call.
- All prior concerns stand: publish on hold for Erika's wording of Articles 1–9, 0008 needs re-scoping if any of them change, sentence-count drift would silently re-point comment anchors, Article 10 still my draft, and the Vercel check needs `--allow-current` unsync + re-sync.

---

## Progress Update as of [2026-07-25 10:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed roborev job 35. The headline finding is one I had shipped and then told Erika to run: **the documented remedy for the frozen-draft build failure could never have worked.** `unsyncVersion` refused any version marked current, and the frozen draft is *always* the current version, so the fix-it command was guaranteed to be refused. Added a narrow `--allow-current` override. Plus two test-quality fixes.

### Detail of changes made:

**The blocked remedy (Medium)**

`sync-versions` passes `isCurrent: entry.version === index.current`, and `versions.json` has `current: "0.1.0"` — so the row frozen with stale hashes is the one with `is_current = true`, and `unsyncVersion` returned `refusedBecause: "this version is marked current — publish a different version first"` before it ever counted dependents. Widening the hash check to all three files (job 26) made this path much easier to hit while the only documented escape stayed blocked.

- **`src/lib/db/unsync-version.ts`** — reordered so **dependents are checked first**: that, not `is_current`, is the real safety property, and no flag may override it. The `is_current` refusal now sits second and is overridable via `opts.allowCurrent`. "Current but referenced by nothing" is precisely a frozen draft. The refusal message now *names the override* and warns that the database is briefly left with no current version.
- **`scripts/unsync-version.ts`** — `--allow-current` flag; usage text explains you will almost always need it and why; prints a loud post-delete warning with the `pnpm sync-versions` follow-up when the deleted row was current.
- **`src/lib/db/sync.ts` + `README.md`** — both now give the *working* three-command sequence instead of the one that gets refused.

**Test quality (both Low, both real)**

- **`tests/lib/bill-of-rights.content.test.ts`** — the agents.md guard only validated bullets that already matched `- **Article N — <title>.**`, so deleting a bullet or reformatting it (hyphen for em dash, dropped period) *removed* that article from the check instead of failing it. Added a second test deriving the required set **independently** from the canonical markdown: every article the current version added relative to the previous one must have a conforming bullet.
- **`tests/lib/db.migration-0008-repoint-comments.test.ts`** — the keyless-leftover test seeded the tables with `AS SELECT … WHERE false`, i.e. *empty*. A real leftover has rows and a nullable `id`, and `ADD PRIMARY KEY` against a populated nullable column is the statement operators actually run. Now seeded from real rows, asserting both backup counts and that the pre-existing mapping survives `ON CONFLICT DO NOTHING` pointing at 0.0.1 (an overwritten backup would silently make the README rollback a no-op).

### Verification

248 tests (40 files), `tsc --noEmit` clean, eslint at the 170-problem baseline. Both new guards verified load-bearing by injecting the regression: deleting the Article 11 bullet produced *"v0.1.0 added Article 11, but v0.1.0.agents.md has no bullet for it"*, and stripping the `DO $$` block reproduced the exact production failure — *"there is no unique or exclusion constraint matching the ON CONFLICT specification"*.

### Potential concerns to address:

- **The corrected unblock sequence** is now: `pnpm tsx scripts/unsync-version.ts 0.1.0 --allow-current --yes` then `pnpm sync-versions`. It will need re-running after the Articles 1–9 rewording lands, since that changes v0.1.0's markdown hash again.
- All concerns from the 10:00 entry still stand — publish on hold for wording, 0008 needs re-scoping if Articles 1–9 change, sentence-count drift would silently re-point anchors, Article 10 still my draft.

---

## Progress Update as of [2026-07-25 10:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Erika: *"I need all the signatures to stay. I can't lose the signatures!"* Investigated and no signature is lost by the publish — every public surface already counts distinct signers across all versions. The real gap was **per-person**: a prior signer had no row against the new version and so read as `not-signed`, getting handed a blank sign form as if they were a stranger. Added a distinct `signed-earlier` state and a one-click re-affirm. Erika chose this over backfilling rows. **The 0.1.0 publish is on hold pending her rewording of Articles 1–9.**

### Detail of changes made:

- **`src/lib/db/signature-status.ts` (new)** — `resolveSignatureStatus(db, signer, versionString)` reads *all* of a signer's signatures rather than filtering to one version, because "signed something else" and "signed nothing" are different answers and only the unfiltered read distinguishes them. Returns `signed` / `signed-earlier` / `not-signed`. `signed-earlier` carries the version they actually signed, the `requestedVersion`, and `firstSignedAt` for "signing since" copy. Ordered `desc(signedAt), asc(id)` — a **total** order, since `signed_at` ties for admin/bulk rows and an undefined order would make the reported version flip between requests for the same person. Extracted into `src/lib/db/` (not left in the `"use server"` file) so it is injectable and testable, matching the `deleteSigner` pattern in `revoke.ts`.
- **`src/server/actions/me.ts`** — `getMySignatureStatus` now delegates to the above. New `reaffirmMySignature(versionString)`, modeled on `submitSignAction`: reuses the signer's stored profile (re-typing their name to say "yes, this one too" is busywork) but renders and hashes a **new** consent record against the new version's text. Duplicate-key is treated as success, since the desired end state already holds.
- **`src/app/SignModal.tsx`** — new `signed-earlier` branch between the already-signed view and the sign form. Shows their name, "signing since <first date> (v0.0.1)", states plainly that their signature still counts and still appears publicly, names the two new Articles, and offers *Add my name to v0.1.0* / *Read what changed first* / *Not now*. Added `reaffirming` state, reset on modal close.
- **`tests/lib/db.signature-status.test.ts` (6 tests)** — including the exact-match-wins case (signed both versions, most recent row is the *older* version) and proof that re-affirming leaves two rows and both versions read as `signed`.
- **`README.md`** — rewrote the signatures bullet under "What does and doesn't carry forward" to state that nothing deletes signatures, list the version-agnostic surfaces by name, and explicitly warn **not** to backfill rows onto a new version.

### Why not backfill

Writing v0.1.0 signature rows for every existing signer would make everyone read as current with no action, but it records consent to Articles 10 and 11 from people who never saw them — on a document whose Article 1 is *"Opt-out is not consent."* Erika chose the re-affirm path.

### Verification

244 tests pass (40 files), `tsc --noEmit` clean, eslint at the 170-problem branch baseline. The central test was verified load-bearing by reverting `resolveSignatureStatus` to the old version-filtered behavior: `expected 'not-signed' to be 'signed-earlier'`.

### Potential concerns to address:

- **The 0.1.0 publish is deliberately on hold.** Erika is sending new wording for Articles 1–9. Do not merge until it lands.
- **Migration 0008 will need re-scoping once that wording arrives.** Its correctness argument is that Articles 1–9 are byte-identical between the versions; that is what makes carrying comments forward safe. For any article whose text changes, its comments must either stay on v0.0.1 or be moved knowing they now sit under different wording. The `tgt`/`src` CTE currently moves *all* of a version's comments with no per-article predicate.
- **Sentence-level anchors are the sharper risk.** Comments anchor to `article-NN-s-M`. Rewording that changes the number of sentences in an article silently re-points every comment after the change to a different sentence. Any reworded article needs its sentence count compared against v0.0.1 before 0008 touches it.
- **Article 10's wording is still my draft**, awaiting Erika's rewrite. Article 11 is her wording, final.
- **The Vercel check stays red** until `pnpm tsx scripts/unsync-version.ts 0.1.0 --yes` is run against the dev Neon branch. Note this will need re-running after the Articles 1–9 rewording lands, since that changes v0.1.0's markdown hash again.

---

## Progress Update as of [2026-07-25 09:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Closed out roborev job 26. Three real defects: `syncVersions` only compared the **markdown** hash, so `agents.md` and `spec.json` could drift silently forever; migration 0008's backup tables could pre-exist **without a primary key** (an earlier revision created them via `CREATE TABLE … AS SELECT`), which would make the `ON CONFLICT ("id")` clause abort the whole migration; and two content files could drift from their sources with nothing to catch it. Each fix was verified by reverting it and watching the new test fail.

### Detail of changes made:

- **`src/lib/db/sync.ts`** — the immutability check now compares **all three** hashes (markdown, `agents.md`, `spec.json`), not just markdown, and the thrown error *names every* mismatching file with both hashes. This mattered because `syncVersions` `continue`s on a match without updating the stored hashes, so a changed `agents.md` was silently accepted and then permanently disagreed with disk.
- **`drizzle/0008_repoint_comments_to_v0_1_0.sql`** — added a guarded `DO $$ … $$` block (step 1b) that adds the missing `PRIMARY KEY ("id")` to either backup table if it lacks one. `CREATE TABLE IF NOT EXISTS` silently no-ops against a keyless table left by the earlier `CREATE TABLE AS SELECT` form, and the failure it prevents is `there is no unique or exclusion constraint matching the ON CONFLICT specification` — verified by removing the block.
- **`tests/lib/db.migration-0008-repoint-comments.test.ts`** — the premature-run test now covers `proposed_edits` symmetrically (`editBackupCount`, `earlyEdit`/`lateEdit`), not just comments.
- **`tests/lib/db.sync.test.ts`** — three new tests: an `agents.md`-only mismatch throws, a `spec.json`-only mismatch throws, and an all-three mismatch names all three in one message.
- **`tests/lib/bill-of-rights.content.test.ts`** — two new drift guards: the `changelog:` frontmatter of each version must match its `versions.json` entry, and every Article title in `v0.1.0.agents.md` must match the canonical markdown. Both verified to catch injected drift.
- **`README.md`** — corrected "before moving anything" to "**in the same statement that performs the move**", which is what 0008 actually does now.
- **`AGENTS.md`** — moved the "Database migrations are applied by hand" section **below** `<!-- END:nextjs-agent-rules -->`. It was inside the externally-managed block and the next regeneration would have deleted it.

### Verification

`vitest run` (231 tests) green, `tsc --noEmit` clean, eslint at the branch baseline of exactly 170 problems (161 errors, 9 warnings).

### Potential concerns to address:

- **The Vercel check is still red** and stays red until the stale v0.1.0 row is cleared from the dev Neon branch: `pnpm tsx scripts/unsync-version.ts 0.1.0 --yes`. Needs `.env.local`, which does not exist in this worktree.
- **Article 10's wording has still not been reviewed by Erika.** It is my draft. Article 11 is Erika's own wording, final.
- **Erika has asked to reword "many of the other articles."** This collides directly with migration 0008, whose entire correctness argument is that Articles 1–9 are byte-identical between v0.0.1 and v0.1.0 — that is what makes carrying comments forward safe. If any of 1–9 change, 0008 must be re-scoped per-article (move only the comments anchored to unchanged articles) or dropped. Do not merge a reworded 1–9 with 0008 as written.
- **Signatures do not migrate, by design, and nothing deletes them.** All public surfaces (`getSignatureCount`, `listSignatures`, the ticker, `/signers`, `/signatories`) count *distinct signers across all versions*, so no signature disappears from any count or list when `current` flips. The one real change is that a person who signed v0.0.1 is not recorded as having signed v0.1.0 and will be offered the sign form again. Whether to backfill v0.1.0 signature rows for existing signers is an open decision — it would assert consent to Articles 10 and 11, which nobody has read.

---

## Progress Update as of [2026-07-24 22:15 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

**The Vercel check went red** after the Article 11 rewrite, and the cause is a workflow trap worth understanding: Vercel *preview* deploys run `sync-versions` too, so the first preview build of this PR froze v0.1.0's text in the dev database. Editing Article 11 afterwards made every later build fail the immutability guard. Added a guarded `unsync-version` script to clear a stale draft row, tests, and README documentation. **Production is unaffected** — previews use the `dev` Neon branch.

### Detail of changes made:

**The failure**

```
Error: Version 0.1.0 hash mismatch:
  existing 85546865… vs new 2d374e1c…
  The canonical document text is meant to be immutable.
  at syncVersions (src/lib/db/sync.ts:36)
```

`postbuild` runs `scripts/sync-versions.ts` on **every** deploy including previews (README "Dev / prod database isolation": Preview scope's `DATABASE_URL` points at the `dev` Neon branch). So the first preview build of this branch inserted v0.1.0 with the *draft* Article 11 text and its markdown hash. Erika's rewrite changed the text, the hash no longer matched, and `syncVersions` — correctly — refused. This is the immutability guard doing its job; the trap is that it also applies to versions still being drafted, and the error surfaces as an opaque build failure rather than "your draft is already frozen".

**The fix**

- **`src/lib/db/unsync-version.ts`** — deletes an unused version row so the next sync re-inserts it from disk. Refuses if the version `is_current`, or if *anything* references it: signatures, comments, proposed edits (both `base_version_id` and `published_in_version_id`), endorsements (both FK columns), attestations, or another version claiming it as `parent_version_id`. A version people have signed or discussed is not a draft and its text is not ours to change. Returns a structured report rather than throwing, so the caller decides what to print.
- **`scripts/unsync-version.ts`** — CLI wrapper. **Dry run by default**; prints the dependency counts and requires `--yes` to actually delete. Connects via `DATABASE_URL` from `.env.local`, which per the README is the dev branch.
- **`tests/lib/db.unsync-version.test.ts` (7 tests)** — including the end-to-end shape that matters: sync → edit → re-sync **throws** `hash mismatch` → unsync → re-sync succeeds and the row now carries the edited text. Plus refusal cases for current, signed, and commented-on versions, and dry run.
- **README** — new "Editing a version's text after a deploy has synced it" section quoting the exact error, explaining *why* previews trigger it, and giving the two commands. Ends by noting that once a version is live and signed, the text is genuinely frozen and changing it means a new version.

**What Erika needs to do:** run `pnpm tsx scripts/unsync-version.ts 0.1.0 --yes` against the dev branch (or delete the row in the Neon console) to make the preview build green. I have no `.env.local` in this worktree and deliberately did not go looking for credentials.

### Potential concerns to address:

- **The Vercel check is red until that row is cleared.** Nothing in the code is wrong; the dev DB is holding a stale draft.
- **The same trap will fire on the production publish if v0.1.0's text is edited again after merge**, because `postbuild` will have synced it to the production DB — and there `unsync-version` will (correctly) refuse once anyone has signed. Article 11's text should be final now; Article 10's has still not been reviewed by Erika.
- The README also references `scripts/rename-version.ts` and `scripts/merge-version-rows.ts`, neither of which exists in `scripts/`. Pre-existing doc drift, not touched here.
- Everything below still stands: two manual migrations pending (0007, 0008); backup tables must be dropped manually and `db:push` avoided until then; signature re-sign UX; unwired `notificationPreference`; four accepted pull-quote divergences.

---

## Progress Update as of [2026-07-24 22:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Erika supplied final wording for Article 11, which also renamed it (dropping "Honest") and replaced its pull quote. Applied across all five places the article appears. Separately, the sixth roborev pass (job 20) found that migration 0008's backup could go stale in exactly the "safe to run early" scenario the file advertises — fixed by moving the snapshot inside the guarded atomic statement.

### Detail of changes made:

**Article 11 final wording (from Erika)**

> **You Have the Right to Safe, Tested Systems.** Before an AI system is used to make a decision about you, it must be tested and shown to be safe and effective. The burden of proving it works falls on the company that builds it, not on you to prove it broke you after the fact.

This is a substantive reframing, not a copy edit. The draft was about capability disclosure ("say plainly what it cannot do"); the final text is about **burden of proof** — testing before a decision is made about you, with the company carrying the evidentiary load rather than the harmed person carrying it retroactively. Changes applied:

- `v0.1.0.md` — title and body replaced; now two anchored sentences (`article-11-s-1`, `-s-2`) instead of five. The closing sentence becomes the homepage pull quote, which is why the exact `body + pullQuote === canonical` test still passes.
- `HomepageArticles.tsx` — title, body, and pullQuote updated to match.
- `v0.1.0.spec.json` — `human_text` updated and **slug changed** `safe-tested-honest-systems` → `safe-tested-systems`. Safe to change: the spec has never been synced to a database, and nothing references principle slugs yet.
- `v0.1.0.agents.md` — the builder-facing directive rewritten around burden of proof rather than limitation-disclosure.
- Changelog strings in `v0.1.0.md` frontmatter and `versions.json`.
- **"Connects to" pill swapped**: `HumaneBench Principle — Honesty` → `HumaneBench as measurement infrastructure` (also in spec.json references). Honesty fit the old "admit what it can't do" framing; the new text is about testing and evidence, which is what the measurement-infrastructure page covers. Article 3 keeps its Honesty link — that one is genuinely about disclosure. **Flagging this as my judgement call, easily reverted if you disagree.**

**Roborev job 20 — migration 0008's backup could go stale (Medium)**

The backups were `CREATE TABLE IF NOT EXISTS … AS SELECT`, i.e. populated at creation, but the *move* is guarded on `is_current`. So on the documented "safe to run early" path the tables would be created and populated with whatever existed at that moment, and `IF NOT EXISTS` meant the real run later would never refresh them. Anything written against 0.0.1 between the two runs would be moved but **absent from the backup**, and the rollback SQL would strand it — the unrecoverable state the backups exist to prevent. Fixed by splitting structure from population: the `CREATE TABLE`s are now empty declarations, and the snapshot happens via `INSERT … ON CONFLICT (id) DO NOTHING` sub-CTEs *inside* the same guarded atomic statement as the move. All sub-statements of a data-modifying CTE share one snapshot, so the INSERTs record pre-update values; `ON CONFLICT DO NOTHING` preserves the first real run's mapping while still capturing rows a premature run missed. **Verified**: new test "captures rows written between a premature run and the real one" fails against the old form with `expected 1 to be +0`.

Three more from the same pass:
- The rollback test only ran the `comments` half of the README's down SQL. The `proposed_edits` half was operator-facing procedure that had never been executed anywhere — a typo would surface during a live rollback. Both halves now run, via a shared `rollback()` helper, asserting the proposed edit returns to 0.0.1.
- The per-statement assertions didn't discriminate — `statements[1]` also contains `proposed_edits`, so the third chunk could have been anything. Now asserts the tokens that encode this migration's safety properties: `moved_comments`/`snap_comments`/`snap_proposed_edits` CTEs, `AND "is_current"`, and that statements 0 and 1 contain **no** `SELECT` (i.e. the backups are not populated at creation).
- **`pnpm db:push` would silently destroy the backups.** They're intentionally not in `schema.ts`, and drizzle-kit treats absent tables as drops. README now warns against running push while they're live, with the `DROP TABLE` statements to use once the publish has settled.

### Potential concerns to address:

- **Article 11's connects-pill swap is my call, not Erika's** — see above.
- Article 10's wording has **not** been reviewed by Erika; only Article 11 was supplied. It is still my draft.
- Everything below still stands: two manual migrations pending (0007, 0008) with 0008 running after the deploy; backup tables must be dropped manually and `db:push` avoided until then; signature re-sign UX; unwired `notificationPreference`; the four accepted pull-quote divergences.

---

## Progress Update as of [2026-07-24 21:45 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Fifth roborev pass (job 17), on the comment-migration commit. Its Medium was the one that mattered: migration 0008 was **destructive, irreversible, and non-atomic**. Now it snapshots the original mapping into backup tables first, moves both tables in a single data-modifying CTE, and only fires while v0.1.0 is actually the current version. Four other Lows fixed alongside.

### Detail of changes made:

- **0008 is now reversible (Medium — fixed).** It overwrote `base_version_id` with no record of what was there. If the publish were rolled back — the normal response to a bad release — every thread would vanish again and "re-point back" could no longer distinguish carried-forward comments from ones genuinely written against v0.1.0. Now `CREATE TABLE IF NOT EXISTS comment_version_backup_0008 AS SELECT id, base_version_id FROM comments WHERE …` (and the same for `proposed_edits`) runs first, capturing the exact set being moved. `IF NOT EXISTS` matters: a re-run must not overwrite the first run's snapshot with a now-empty set. The **down SQL is documented in the README** under a new "Rolling back the 0.1.0 publish" heading, and a test executes that exact SQL and asserts the threads land back on v0.0.1.
- **0008 is now atomic (Medium — same finding).** It was two separate `UPDATE`s, and `scripts/apply-migration.ts` sends each statement as its own neon-serverless HTTP request — one implicit transaction per call — so a failure between them would leave `comments` and `proposed_edits` in different version scopes. Both moves now happen in a single data-modifying CTE (`WITH tgt … moved_comments AS (UPDATE comments …) UPDATE proposed_edits …`).
- **The `EXISTS` guard now requires `is_current` (Low — fixed).** It only checked that a row named `0.1.0` existed. Deploy order here is manual, so if 0.2.0 shipped before anyone ran 0008 in production, the migration would have silently moved every comment onto a non-current version: threads stay hidden *and* the original scoping is destroyed — strictly worse than not running it. The target subqueries are now `WHERE version = '0.1.0' AND is_current`, making an out-of-order run a no-op. Test added for exactly that three-version scenario.
- **Shared migration splitter (Low — fixed).** The test hand-rolled its own splitting and claimed to match `scripts/apply-migration.ts`, but didn't: the script keeps comment lines *inside* a chunk and falls back to `;`-splitting when there's no breakpoint marker, while the test stripped every `--` line and always split on the marker. The two could therefore disagree about what production executes — the same "hand-rolled parser instead of the one being guarded" pattern flagged a round earlier. Extracted `src/lib/db/split-migration.ts`, imported by both. **Verified** the shared splitter yields 2/3/4 statements for 0007/0008/0006 respectively, matching the script's previous behavior.
- **The second statement is now actually asserted (Low — fixed).** No test seeded a `proposed_edits` row, so deleting the entire second `UPDATE` — or mangling a `--> statement-breakpoint` so chunks collapsed — left every test green. Now `expect(statements).toHaveLength(3)` with per-statement content checks, plus a test that seeds a proposed edit and asserts its `base_version_id` moves (and doesn't move in the no-target and not-current cases).
- **`db.execute(statement as never)` → `sql.raw(statement)` (Low — fixed).** The cast suppressed the parameter type entirely and would have hidden a genuine signature change, including pglite's documented multi-command restriction.

Test count for this file went 5 → 9; suite total is 223.

### Potential concerns to address:

- **The backup tables are left in place deliberately** so rollback stays possible. Drop `comment_version_backup_0008` and `proposed_edit_version_backup_0008` once the publish has settled — noted in the README.
- Deploy steps are unchanged: apply `0007` then `0008` after the deploy, once `sync-versions` has made v0.1.0 current.
- Everything in the entries below is still open: signature re-sign UX, unwired `notificationPreference`, the four accepted pull-quote divergences, and editorial review of Articles 10/11.

---

## Progress Update as of [2026-07-24 21:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Fourth roborev pass (job 15). Its HIGH was the comment blocker, already resolved by the previous commit. The six Lows were all real — most notably that equal `signed_at` timestamps defeat the ticker de-duplication I'd just added, which the comment-migration option makes *more* likely rather than less. Fixed all six.

### Detail of changes made:

- **Equal timestamps defeated the ticker fix (Low, but the sharpest one).** The "is this their first signature?" probe used a strict `earlier.signed_at < s.signed_at`. Two rows for the same signer with *identical* timestamps each see the other as not-earlier, so both qualify as first-time and the person is announced twice — precisely the failure the exclusion exists to prevent. Now a total order: `earlier.signed_at < s.signed_at OR (equal AND earlier.id < s.id)`. Test added; **verified it fails without the fix**, returning `['Simultaneous','Simultaneous']`. Worth noting roborev's reasoning: equal timestamps aren't hypothetical here because bulk/admin-created signatures stamp a fixed time, and a backfill copying signature rows forward would produce them at scale.
- **`DISTINCT ON` needed the same tiebreaker (Low — fixed).** I'd added `signerId` to the *outer* ORDER BY but left the DISTINCT ON sort as `(signerId, signedAt DESC)`. With two same-timestamp signatures, which row survives is undefined — so the `version` rendered next to that person's name on `/signers` and `/signatories` was arbitrary and could change between page loads. Now `(signerId, signedAt DESC, id DESC)`, with a test that reads five times and asserts agreement.
- **Second index for the ticker (Low — fixed).** `0007` only added `(signer_id, signed_at DESC)`, which serves the DISTINCT ON sort and the correlated probe — but `listRecentSignersSince`'s *driving* predicate is `signed_at > cutoff` with no `signer_id` restriction, which that index cannot serve. `/api/signers/recent` is `force-dynamic` and polled roughly once a minute by every open homepage tab, making it the hottest of these shapes. Added `signatures (signed_at DESC)` to the migration, `schema.ts`, and the pglite test schema.
- **Missing ticker test case (Low — fixed).** Both existing tests put the re-signer's first signature outside the window (48h) or omitted it. The case where first signature *and* re-signature both fall inside the 60-minute window was untested — and it's what an "exclude anyone whose earliest signature predates the cutoff" implementation would get wrong. Added, including an assertion that the announcement carries the *first* signature's timestamp.
- **Raw `sql` → `lt()` (Low — fixed).** The correlation predicate was a raw template beside an `eq()`. Now uses `lt()`/`or()`, so a renamed or retyped column is a compile error rather than silently-wrong SQL.
- **Unjournaled-migration convention documented (Low — fixed).** `_journal.json` stops at idx 4 while 0005–0008 exist as loose files, so `drizzle-kit migrate` won't apply them and the only record that they must be run by hand was a commit message. Added a **"Database migrations are applied by hand"** section to `AGENTS.md` covering: use `scripts/apply-migration.ts`; write migrations idempotent because re-running is normal here; a new migration file does not apply itself and nothing in CI or the Vercel build runs it; and `drizzle-kit generate` will re-emit unjournaled migrations. It ends with a **pending-migrations list** naming 0007 and 0008 and the ordering constraint on 0008.

### Potential concerns to address:

- **Two manual migrations are now pending** (`0007`, `0008`), listed in both `AGENTS.md` and `README.md`. 0008 must run after the deploy's `sync-versions`.
- `getSignatureNumber` still uses a raw `sql` template for its `<=` comparison (pre-existing, untouched). Same class as the `lt()` cleanup above; left alone to keep this diff scoped.
- Everything in the entries below is still open: signature re-sign UX, unwired `notificationPreference`, the four homepage pull-quote divergences (accepted by decision), and editorial review of Articles 10/11.

---

## Progress Update as of [2026-07-24 21:15 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

**The blocker is resolved.** Erika chose to migrate existing comments forward to v0.1.0 (option a of the three), and to leave the four divergent homepage pull quotes as-is under the documented test exemption. Added `drizzle/0008_repoint_comments_to_v0_1_0.sql`, five tests that run the real SQL file against pglite, and a README section on what does and doesn't carry forward across a version bump. **PR #31 is now unblocked.**

### Detail of changes made:

- **`drizzle/0008_repoint_comments_to_v0_1_0.sql`.** Re-points `comments.base_version_id` from the v0.0.1 row to the v0.1.0 row, looking both up by version string rather than hardcoding uuids. Also re-points `proposed_edits` (no rows today — the feature is schema-only — but the two are scoped identically and drifting apart would be a nasty surprise later). **`endorsements` is deliberately excluded**: endorsing v0.0.1 is a statement about *that* version's text, and the table is unique on `(signer_id, base_version_id)` so re-pointing could collide outright. Every statement is guarded by `EXISTS (SELECT 1 FROM versions WHERE version = '0.1.0')`, so running the migration before `sync-versions` has created the row is a no-op rather than a blanked-out `base_version_id`.
- **Why this is safe here specifically:** Articles 1–9 are textually identical between the two versions (enforced by the carry-over test) and Articles 10–11 were appended rather than inserted, so every existing `article-NN-s-M` anchor still resolves to the same sentence. The cost of this option is losing the record of which version's text each comment was written against — acceptable precisely because the text under those comments did not move.
- **`tests/lib/db.migration-0008-repoint-comments.test.ts` (5 tests).** Runs the actual `.sql` file — split on `--> statement-breakpoint` the same way `scripts/apply-migration.ts` does — against an in-memory Postgres, rather than reimplementing the SQL in the test. Covers: the bug itself (without the migration, `listCommentsForVersion` on the new version returns nothing); the fix (the old thread becomes visible and `countCommentsByAnchor` resolves it); comments already on the new version are untouched; running it twice is safe; and running it before the new version row exists leaves the data alone. **Verified load-bearing** by pointing the migration at a nonexistent source version — three tests failed.
- **README publishing section rewritten.** It previously said only that signatures don't migrate. Now has a "What does and doesn't carry forward" subsection covering signatures (don't migrate; counts are distinct-by-person so re-signing never double-counts), comments (don't migrate on their own — this is the trap, with the condition under which re-pointing is safe), and endorsements (deliberately left behind), plus an explicit **post-deploy step listing both `0007` and `0008`** with the `scripts/apply-migration.ts` invocation, since this repo applies migrations by hand and the drizzle journal is not the source of truth.
- **Pull quotes: no change**, per Erika's decision. The four legacy divergences stay documented in `KNOWN_PULLQUOTE_DIVERGENCES` with the test that prevents new ones. The Article 06 "license plate" line remains on the homepage without being in the signed document — recorded below as a known, accepted state rather than an oversight.

### Potential concerns to address:

- **Deploy order is load-bearing and manual.** `0008` must run *after* the deploy, once `sync-versions` (postbuild) has created the v0.1.0 row. It's a safe no-op if run early, but the comments stay hidden until it runs. `0007` should also be applied. Both are in the README.
- **Article 06's homepage pull quote is still not in the signed document** (the AI-agent "license plate" line), along with 01/02/05 divergences. Accepted for now by decision; the test exemption keeps it from spreading. Worth folding into a future version or correcting on the homepage when there's editorial time.
- **Signature re-sign flow is still a cold sign form.** Everyone who signed v0.0.1 will be asked to sign again with no "you signed the previous version" framing. Worth a follow-up.
- **`notificationPreference` is still unwired** — publishing 0.1.0 is the "minor revision" event it promises and no email goes out.
- **Article 10/11 copy has still not been through editorial review.** Once v0.1.0 syncs to production its markdown hash locks and the text cannot be changed without a new version.

---

## Progress Update as of [2026-07-24 21:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Third roborev pass (job 12). It found a **fourth** count/list surface I'd missed when I claimed "all three surfaces now agree" — the live homepage ticker — plus a performance and a pagination-correctness gap in the query I'd just rewritten. All fixed. The HIGH comment-scoping blocker is still open and still needs Erika's decision; roborev restated it for the third round so it doesn't get lost.

### Detail of changes made:

- **The homepage ticker announced re-signatures (Medium — fixed).** `/api/signers/recent` returns `listRecentSignersSince()` beside `getSignatureCount()`. I'd deduped the count but not the ticker feed, so publishing v0.1.0 would have produced exactly the wrong visible behavior: a name pops into the ticker as though someone just signed, while the counter next to it doesn't move — and anyone who re-signs gets announced to the whole homepage a second time. `listRecentSignersSince` now excludes signatures from people who have an earlier signature (`notExists` over an `aliasedTable(signatures, "earlier_signatures")` correlated on `signer_id` with `signed_at <` the candidate row), so the ticker announces first-time signers only. Two tests: one asserting a re-signer inside the window is not announced while a genuine newcomer is (failed first with `['Newcomer','Alice']`), one asserting a first-time signer of a *later* version still is — that second case is the one a naive "only count v1 signatures" fix would break.
- **Index for the new query shapes (Low — fixed).** The only index on `signatures` was the `(signer_id, version_id)` unique — it leads with `signer_id` but carries no `signed_at`, so my `DISTINCT ON` rewrite forced a full join plus two sorts on every load of `/signers` and `/signatories` (both `force-dynamic`). My previous note that "the join columns are indexed" was true only of the join keys, not the sorts. Added `signatures (signer_id, signed_at DESC)` to `schema.ts`, a matching `drizzle/0007_signatures_signer_signed_at_idx.sql`, and mirrored it in the pglite test schema. **This migration must be applied in production** — the journal at `drizzle/meta/_journal.json` stops at idx 4 while 0005/0006 exist as files, so this project applies later migrations by hand (`scripts/apply-migration.ts`).
- **Pagination tiebreaker (Low — fixed).** The outer `ORDER BY signedAt DESC` had no tiebreaker, so rows with identical `signed_at` (bulk/admin-created signatures) had undefined relative order and a person could appear on two consecutive pages or on neither as `OFFSET` advanced. Now `ORDER BY signed_at DESC, signer_id`. This matters more than it used to: since the post-fetch dedupe came out, the offset window is the only pagination mechanism.
- **`signedAt` type is now asserted (Low — fixed).** It comes back through an aliased subquery, and both `/signers` and `SignatureCard` call `.toISOString()` on it directly — a string would throw at render while the suite stayed green. The dedupe test now asserts `toBeInstanceOf(Date)` and checks both rows' exact timestamps.
- **Resource markdown guard widened (Low — fixed).** It only checked the body, but the page also renders `title` and `subtitle` as text nodes, and the marker list missed `_underscore_`, `> blockquote`, `1. ordered`, and setext underlines. Now goes through `getResource()` — the same parser the page uses, so it can't drift from the real frontmatter handling — over title + subtitle + body, with the wider marker set. **Verified by probe:** injected each of `_emphasis_`, `> quote`, `1. item`, `## heading` into a resource body and `**bold**` into a `subtitle:` line; all five were caught, then reverted.
- **Test helper duplication removed (Low — fixed).** The two new tests had ~45 lines of byte-identical `seed`/`sign` helpers differing only in a `clerkUserId` prefix. Hoisted to module scope as `seedSigner` / `seedSignature` / `seedResignScenario` (the shared Alice-and-Bob fixture), typed with the exported `TestDb` rather than `any` so lint stays at baseline. Renamed the `listRecentSignersSince` block's local helper to `seedSignerWithSignature` to stop it shadowing the module-scope one.
- **`describe` nesting corrected (Low — fixed).** The new `resource pages` block had been inserted mid-file, so two homepage tests ended up reporting under it. Moved back.
- **`getSignerCount()` documented (Low — fixed).** It lost its last caller when `/signers` switched to `getSignatureCount()`. Kept, with a comment stating it counts `signers` rows *including admin-added non-signers* and is not the public signature count — so the next author reaching for "how many signers" doesn't grab the wrong one.

### Potential concerns to address:

- **BLOCKING, unchanged for the third review round — publishing v0.1.0 hides every existing homepage comment.** Full analysis and the three options are in the 20:30 entry. **PR #31 must not merge until Erika picks one.**
- **`drizzle/0007` must be applied to production** before or with this deploy. Missing it doesn't break correctness — the queries still return right answers — but `/signers` and `/signatories` degrade to full scans.
- Everything in the entries below is still open.

---

## Progress Update as of [2026-07-24 20:45 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Second roborev pass (job 7) on the review-fix commit. It caught that my own `countDistinct` fix had **created an inconsistency**: I deduplicated the signature *counts* but not the signer *lists* they sit next to, so a re-signer would appear twice in a list headed by a number that counted them once. Fixed that at the query level, plus four test-robustness findings. The HIGH comment-scoping blocker is unchanged and still needs Erika's decision.

### Detail of changes made:

- **`listSignatures` now returns one row per person (Medium — fixed).** This is the one I caused. `getSignatureCount()` became distinct-by-signer in the previous commit, but `listSignatures` still returned one row per signature row, so on `/signatories` a re-signer renders twice directly beneath a header that counts them once — and the `key` was `signerId + version`, so React wouldn't even flag the collision. Rewrote the query as `selectDistinctOn([signatures.signerId])` ordered by `signerId, signedAt DESC`, wrapped in an outer query that applies the newest-first ordering and the page window (DISTINCT ON requires its own ORDER BY to lead with the distinct column, so the two orderings can't share one query). Test written first — failed with `['Alice','Bob','Alice']` — then fixed.
- **`/signers` post-fetch dedupe removed (Medium — same finding).** It deduped only *within* the fetched page, so a 100-row page could render fewer than 100 people while the offset still advanced by 100 — silently skipping signers between pages. Now unnecessary: the query returns 100 people for a 100-row page.
- **`/signers` header count corrected (Medium — same finding, pre-existing).** It used `getSignerCount()`, which counts rows in `signers` — including admin-added non-signers created by `createNonSigner` in `src/server/actions/admin.ts:338`, who have a signer row but no signature. On a page headed "Verified signers" listing only people who signed, that over-reports. Switched to `getSignatureCount()`. All three surfaces (`/`, `/signatories`, `/signers`) now report the same number.
- **`getSignatureNumber` regression guard (Low — fixed).** The earlier test used one signer and identical timestamps, so it couldn't detect whether step 1 picks the signer's *earliest* signature — flipping `asc` to `desc` would have passed while every re-signer's public "Signer #N" jumped. Added a two-signer case with distinct timestamps: A signs v1 first, B signs v1 second, A re-signs v2 last; asserts A is #1 and B is #2. It passed on the first run, confirming the existing `asc` ordering is correct — it's a guard, not a fix.
- **`previousVersion()` bounds check (Low — fixed).** Returned `history[idx - 1]` unguarded, so a first-ever version (idx 0) would die with `Cannot read properties of undefined` instead of a readable skip. Now returns `null` and the carry-over test early-returns.
- **Exemption-list dereference (Low — fixed).** The honesty test did `findIndex` then dereferenced without checking `-1`, so a renumbered article failed as a `TypeError` reading like a broken test rather than "the exemption list is stale." Now asserts the index with a message naming the fix.
- **Markdown-in-resources guard (Low — fixed).** The four files were de-emphasized by hand with nothing stopping the next author from reintroducing markdown into a renderer that emits plain text nodes — exactly the drift class the rest of this branch added tests for. New `describe("resource pages")` block checks every file in `content/resources/` for emphasis, headings, links, list items, and backticks, naming the offending syntax in the failure message. All 33 files pass.
- **Prefix test narrowed (Low — fixed).** It was fully implied by the new exact `body + pullQuote` check for the seven non-exempt articles. Now scoped to the four exempted ones, where it's their only coverage, with a comment saying why.

### Potential concerns to address:

- **BLOCKING, unchanged — publishing v0.1.0 hides every existing homepage comment.** Roborev restated it in job 7 to keep it from getting lost between reviews. Full analysis and the three options are in the 20:30 entry below. Nothing about this has been decided or changed. **PR #31 must not merge until Erika picks an option.**
- `listSignatures` now runs a subquery per page load. Fine at current scale (hundreds of signatures) and the join columns are indexed, but worth watching if signature volume grows by orders of magnitude — `DISTINCT ON` over a large `signatures` table with a `LIMIT` in an outer query can't use an index-only scan for the window.
- Everything in the entries below is still open.

---

## Progress Update as of [2026-07-24 20:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Addressed the roborev review (job 3) on the v0.1.0 publish commit. Fixed four findings; one HIGH finding is real, confirmed, and **deliberately left open because it needs a product decision** — see concerns. PR #31 is open and must NOT merge until that decision is made.

### Detail of changes made:

- **Literal asterisks in the new reference pages (Medium — fixed).** `src/app/resources/[slug]/page.tsx` splits the body on blank lines and renders each chunk as a plain React text node; there is no markdown renderer. My four new files were the only ones of the 33 in `content/resources/` using `**bold**` / `*italic*`, so readers would have seen the raw markers. Stripped all emphasis from `eu-ai-act-high-risk-systems.md`, `us-civil-rights-anti-discrimination-law.md`, `nist-ai-risk-management-framework.md`, and `model-cards-and-system-documentation.md`. Re-read each affected sentence — all still parse as prose without the emphasis.
- **Signature count double-counting (Medium — fixed).** `signatures` is unique on `(signer_id, version_id)`, so one person signing both v0.0.1 and v0.1.0 makes two rows. `getSignatureCount()` counted rows and feeds the public "N signatures" / "N other real people" copy; `getSignatureNumber()` had the same flaw feeding "You're Signer #N". Both now use `countDistinct(signatures.signerId)` (`src/lib/db/queries.ts`). This was latent-but-harmless while only one version existed; publishing 0.1.0 activates it. Wrote the test first (`tests/lib/db.queries.test.ts` — "counts people, not rows, across versions"), watched it fail `expected 2 to be 1`, then fixed.
- **Pull-quote drift untested (Medium — fixed, and it surfaced a real content bug).** The original drift test only checked that `body` was a *prefix* of the canonical text and never checked `pullQuote` at all. Replaced with an exact `body + " " + pullQuote === canonical` assertion. **Four articles already diverge and are exempted by number in `KNOWN_PULLQUOTE_DIVERGENCES`** — see concerns. A second test asserts every exempted article really does still diverge, so the exemption list cannot outlive the problem. Articles 03, 04, 07, 08, 09, 10, and 11 match exactly.
- **Test hardcoded version literals (Low — fixed).** `it("names 0.1.0 as the current version")` and the v0.0.1 carry-over check would have needed hand-editing every release, and the first would have failed on 0.2.0 for no reason. Both now derive from `versions.json`: current must equal the newest history entry, and the carry-over check diffs against the immediate predecessor and asserts *every* prior article survives, not a hardcoded 1–9 range.

Not addressed (judged out of scope, both already recorded as concerns below and in PR #31): the current version being hardcoded in nine places rather than resolved once server-side, and the unwired `notificationPreference`.

### Potential concerns to address:

- **BLOCKING — publishing v0.1.0 hides every existing homepage comment.** Confirmed in code, not theoretical. `listCommentsForVersion` / `countCommentsByAnchor` / `listThreadedCommentsForVersion` all filter `eq(comments.baseVersionId, baseVersionId)` (`src/lib/db/queries.ts:184`, `:214`, `:252`), and `loadHomepageTabData` sets `baseVersionId` from `getCurrentVersion()`. Every comment ever posted was written against the v0.0.1 version row, so the moment `sync-versions` runs in production they all vanish from `/` and `/proposed`. The same applies to `proposed_edits.base_version_id` and to `endorsements`, which is unique on `(signer_id, base_version_id)`. Appending Articles 10/11 protected the `article-NN-*` anchor ids but anchors are not what gates the query. **Three options, none of which should be picked without Erika:** (a) migrate existing `comments` / `proposed_edits` rows to point at the new version id — carries discussion forward but loses the record of which text was being discussed; (b) query comments across all versions and match on anchor id — keeps provenance, needs a query change and a decision about showing v0.0.1 comments on v0.1.0 text; (c) accept the reset as intended — each version gets a fresh discussion — in which case it must be stated in the release notes, because nothing in the diff records it as a choice.
- **Four homepage pull quotes are not in the signed document.** Surfaced by the strengthened test, pre-existing, not introduced here. Article 01 canonical closes "The default is no." but the homepage shows 'The default is "No LLM training on my data"'; article 02 "Memory built on your life is yours." vs "LLM memory built on your life is yours."; article 05's pull quote is a paraphrase of a sentence with no closing line; and **article 06's pull quote — about every AI agent having a "license plate" identifier traceable to a responsible human — appears nowhere in the canonical document at all.** People are reading these on the homepage as part of the Bill. Either add the text to a future version or correct the homepage; this is an editorial call.
- Everything below carries over from the previous entry and is still open.

---

## Progress Update as of [2026-07-24 20:00 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

First entry on this branch. Published **v0.1.0** of the AI Bill of Rights, which adds two new principles — Article 10 (Freedom From Algorithmic Discrimination) and Article 11 (You Have the Right to Safe, Tested, Honest Systems) — taking the document from 9 principles to 11. Articles 1–9 carry over from v0.0.1 verbatim. v0.1.0 is now the *current* version: `content/bill-of-rights/versions.json` points at it, every hardcoded `"0.0.1"` default in the app was bumped to `"0.1.0"`, and all "Nine commitments" site/email copy became "Eleven commitments". Added four new `content/resources/*.md` reference pages backing the new articles' "Connects to" pills, and a new test file that guards against the two content sources drifting apart.

### Detail of changes made:

**New content (v0.1.0)**
- `content/bill-of-rights/v0.1.0.md` — the canonical document. Preamble + Articles 1–11, each sentence carrying an `{#article-N-s-M}` anchor as the parser (`src/lib/markdown/parse.ts`) requires. Articles 1–9 are byte-for-byte the text from v0.0.1; a test asserts this.
  - **Article 10: Freedom From Algorithmic Discrimination** — protected characteristics across hiring/lending/housing/healthcare/policing/insurance/education; disparate impact counts absent intent; test-before-deploy and publish findings. Pull quote: "No AI may treat you differently for who you are."
  - **Article 11: You Have the Right to Safe, Tested, Honest Systems** — test for real-world use, state limits plainly, no unevaluated capabilities, no confident answers in unvalidated domains, publish failure modes and error rates pre-deployment. Pull quote: "It has to work, and admit what it can't do."
- `content/bill-of-rights/v0.1.0.spec.json` — **expanded beyond the v0.0.1 stub**. v0.0.1's spec listed only principle 1; v0.1.0 lists all 11 with `id`, `slug`, `human_text`, and `references` mirroring the site's "Connects to" pills. `prohibited_behaviors` / `required_behaviors` / `test_conditions` remain empty arrays — still the editorial council's to author, per the `_note`.
- `content/bill-of-rights/v0.1.0.agents.md` — stub carried over from v0.0.1 plus a new "What changed since v0.0.1" section giving builders concrete directives for Articles 10 and 11 (no proxies for protected characteristics; disparate-impact testing before ship; don't ship unevaluated capabilities; surface uncertainty/refusal in-product).
- `content/bill-of-rights/versions.json` — `current` flipped `0.0.1` → `0.1.0`; 0.1.0 appended to `history`. **v0.0.1's three files are untouched** — `syncVersions()` in `src/lib/db/sync.ts` hashes the markdown and throws on any change to an already-synced version, so published versions are immutable by design.

**New reference pages** (`content/resources/`, consumed by `src/lib/resources.ts` → `/resources/[slug]`)
- `eu-ai-act-high-risk-systems.md` — Annex III high-risk contexts, Article 10 data-governance/bias-examination duties, outcome-not-intent framing.
- `us-civil-rights-anti-discrimination-law.md` — Title VII / ECOA / FHA / ADA / ADEA reach algorithmic decisions; disparate-impact doctrine; why "we never fed it race" is not a defense; EEOC + CFPB guidance.
- `nist-ai-risk-management-framework.md` — AI RMF 1.0 Govern/Map/Measure/Manage, contextual validity, documenting limits as a requirement, GenAI Profile (AI 600-1).
- `model-cards-and-system-documentation.md` — Mitchell et al. model cards, Gebru et al. datasheets, why disaggregated performance is the load-bearing part, and the voluntary/unaudited gap this Article targets.
- Articles 10 and 11 also reuse existing pages: `humanebench-principle-dignity`, `white-house-ai-bill-of-rights-2022`, `humanebench-principle-honesty`, `eu-ai-act-conformity-assessments`.

**App changes**
- `src/app/HomepageArticles.tsx` — appended articles `"10"` and `"11"` to the exported `articles` array with `body`, `pullQuote`, and `connects` pills. **Appended, not inserted**, on purpose: comments are keyed to anchor ids derived from `article.number` (`article-NN-s-M`), so renumbering existing articles would orphan every existing comment.
- Version-default bumps `0.0.1` → `0.1.0`: `SignModal.tsx` (`VERSION`), `admin/signers/AdminAddSignerForm.tsx` (`VERSION`), `sign/{complete,consent,profile}/page.tsx` (searchParam defaults), `server/actions/profile.ts`, `server/actions/me.ts`, `lib/homepage/load-tab-data.ts`, `app/bill-of-rights/page.tsx`, `scripts/test-admin-broadcast.ts`. The comment-only example in `src/proxy.ts` was left alone.
- `SignModal.tsx` — the "Minor revisions" notification hint was a literal `v0.0.1 → v0.1.0`, which is now the change that just shipped; updated to `v0.1.0 → v0.2.0`.
- Copy `Nine → Eleven`: `app/page.tsx` (hero + closing), `app/proposed/page.tsx` (hero + closing), `app/signatories/[id]/page.tsx`, `SignModal.tsx` share text, `lib/email/templates.ts` (`signConfirmation` share text + `signInvitation` body).

**New test** — `tests/lib/bill-of-rights.content.test.ts` (16 tests)
The document exists in two places that must agree: the canonical markdown (rendered at `/v/<version>` via the DB) and the hand-maintained `articles` array in `HomepageArticles.tsx` (which the homepage uses because it needs per-sentence comment anchors the markdown renderer doesn't emit). Nothing enforced agreement before. The new tests assert: versions.json names 0.1.0 and every history entry has all three files on disk; the current document parses to preamble + `article-1..11` with correctly namespaced sentence anchors; Articles 1–9 match v0.0.1 verbatim; spec.json has 11 uniquely-slugged principles each with references; the homepage array matches the canonical article count, numbering, and titles, and each `body` is a prefix of the canonical text; **every `connects` slug resolves to a real file in `content/resources/`**. Verified these fail on drift by temporarily typo-ing a slug and a title — both caught.

**Verification run**
- `vitest run` — 37 files, **202 tests, all passing**.
- `tsc --noEmit` — clean.
- `eslint` — 170 problems, **identical to the pre-existing count on a clean tree** (verified by stashing); zero introduced. My changed files contribute none of them.
- `next build` was NOT run: it requires real Clerk keys at build time (see `feat/phase-1-signable-mvp.md` — Clerk validates the publishable key's decoded URL during static prerender), and no `.env.local` is present in this worktree.

### Potential concerns to address:

- **Signatures do not carry forward, by design.** `getMySignatureStatus(VERSION)` filters signatures by exact version string. Everyone who signed v0.0.1 now reads as `not-signed` for v0.1.0 and will be shown the sign form again. This is arguably the correct semantic for a new version, but it means the public signature count for the *current* version starts at zero until people re-sign. Decide whether the homepage `<SignatureCount />` should count all versions or just the current one, and whether existing signers get a "re-affirm" flow rather than a cold sign form.
- **No signer notification exists.** `SignModal` collects a `notificationPreference` of `major` / `minor` / `none`, but nothing consumes it. Publishing 0.1.0 is exactly the "minor revision" event that preference promised, and **no email will go out**. Either build that send or accept that the preference is currently decorative.
- **The two content sources are still duplicated**, just now tested. `HomepageArticles.tsx` holds a second copy of the article text. The new test catches divergence but the real fix is deriving the homepage array from the parsed markdown — blocked on the markdown parser not emitting the `pullQuote` / `connects` metadata the homepage needs. Consider moving pull quotes and pill slugs into the spec.json and rendering from there.
- **`postbuild` runs `sync-versions`.** The new version row only lands in the DB when `tsx scripts/sync-versions.ts` runs against a real `DATABASE_URL`. On Vercel that happens automatically via `postbuild`; for any manual/preview environment it must be run by hand or `/v/0.1.0` will 404 and `getCurrentVersion()` will keep returning 0.0.1.
- **Article 10 and 11 body copy is a first draft** written to match the house voice of Articles 1–9 and expanded from the two one-line summaries in the source mock. It has not been through editorial review. `prohibited_behaviors`, `required_behaviors`, and `test_conditions` are still empty for all 11 principles.
- **`bumpPatch()` now yields `0.1.1`** as the "proposed" version label on `/proposed`. Since 0.1.0 was itself a minor bump adding articles, the next proposed round may want to be `0.2.0`; `bumpPatch` in `src/lib/homepage/load-tab-data.ts` can only ever bump the patch digit.

---
