# Branch Progress: sparkle/agent-2ec73a24-f47e-4104-88cc-b6e8514951f3

## Progress Update as of [2026-07-28 13:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

"Connects to" pill colour is now a property of the pill's *category* rather than a hash of
its slug, and the Proposed tab renders the same colours as Current instead of forcing
grayscale. Both were user requests. Added first-time coverage for pill colouring, which had
none.

### Detail of changes made:

- **Replaced the hash palette in `src/app/HomepageArticles.tsx`.** The old `pillColor()` was
  a `*31` hash of the slug mod a 10-colour palette. Stable per pill, but it spread the six
  HumaneBench principles across five colours (amber, violet, indigo, orange, sky) and
  collided unrelated pills onto the same colour, so the colour carried no information.
  Now `PILL_CATEGORIES` — an ordered list of `{ id, matches, className }` — assigns one
  colour per family:
  - `humanebench` → **sky** (light blue, as specified) — the six principles plus
    `humanebench-as-measurement-infrastructure`, which the user asked to read as one family
    with them.
  - `eu-regulation` → violet (`eu-ai-act*`, `gdpr*`)
  - `us-law` → amber (explicit `US_LAW_SLUGS` set — the slugs share no usable prefix:
    `coppa`, `ftc-*`, `eeoc-*`, `nist-*`, `white-house-*`, `california-*`,
    `emerging-state-ai-legislation`, `consumer-protection-law`)
  - `uk-regulation` → teal (`uk-*`)
  - `research-advocacy` → rose (everything else)
- **`research-advocacy` matches everything and is last**, so a pill can never render
  unstyled. To stop that being a silent catch-all, the test
  `"lands every pill in a category deliberately"` pins its membership to an explicit
  seven-slug list — a newly added slug nobody classified fails there rather than quietly
  inheriting rose.
- **Deleted the `mode === "interactive"` grayscale branch** at the pill className. That was
  a deliberate earlier decision (commit `6d64fca`: "grayscale pills… keeping the colorful
  pastels only on the Current tab"), taken because the Proposed tab paints cyan comment
  highlights over the pill text. Both tabs now call `pillColor(pill.slug)`.
- `pillColor` and the new `pillCategory` are exported so the tests can assert against the
  mapping directly rather than only through rendered markup.
- **Fixed a stale pointer in `tests/lib/db.migration-0010-repoint-comments.test.ts`.** Its
  header cited `HomepageArticles.tsx:539` and `:604` for the anchor-id templates; this
  change shifted both by ~50 lines. Replaced the line citations with the symbol names, since
  the line numbers had already gone stale once before this.

### The cyan-highlight overlap — checked, and it is a real but narrow problem

The grayscale existed for a reason, so I measured it rather than assuming. Injected the three
highlight shades over three pill colours in the running app and read the computed values:

| pill | highlight | ΔE (CIE Lab) | verdict |
|---|---|---|---|
| sky-50 (HumaneBench) | cyan-100 (inactive) | ~12 | **weakest pairing — subtle** |
| violet-50 (GDPR) | cyan-200 (nested) | ~30 | clear |
| amber-50 (US law) | cyan-300 (active) | ~48 | obvious |

So: active highlights read clearly on every category, and every category except HumaneBench
reads clearly at every intensity. The one weak case is an **inactive** highlight on a
**light-blue** pill — cyan and sky are adjacent hues. ΔE ~12 is above the ~2.3 just-noticeable
threshold, so it is visible, but it is much fainter than elsewhere.

Not fixed, because every fix is a decision outside what was asked:
- changing HumaneBench off light blue contradicts the explicit request;
- bumping the inactive highlight `cyan-100` → `cyan-200` darkens comment highlights
  **site-wide**, not just on pills;
- giving highlights a ring/underline so they don't rely on background contrast is the most
  robust option but redesigns the commenting affordance.

Reported to the user with the evidence for them to choose.

### Verification run

- `pnpm test` — 88 files, 932 tests passing (up from 918; 14 new).
- `pnpm tsc --noEmit` and `pnpm lint` on the touched files — clean.
- **Mutation-checked both halves**, since a colour test that cannot fail is worthless:
  restoring the grayscale branch fails 4 render tests; giving one HumaneBench principle a
  different category fails 3 tests across both files.
- Browser-verified on `localhost:3000`: Current and Proposed both show HumaneBench light
  blue, GDPR violet, emerging-state-legislation amber.

### Potential concerns to address:

- **`US_LAW_SLUGS` is a hand-maintained set.** Unlike the other categories it has no prefix
  rule, so a new US-law pill must be added there by hand or it silently becomes
  `research-advocacy` rose. The deliberate-categorisation test catches this, but only
  because it pins the rose membership list — if someone loosens that test, the guard is gone.
- **Two of the eight official HumaneBench principles still have no pill**
  (`enhance-human-capabilities`, which has an orphaned resource file, and
  `foster-healthy-relationships`, which has none). Unchanged by this work, but the colour
  grouping makes the gap more visible now that the family reads as a set.
- The category ids are not surfaced in the UI — there is no legend telling a reader that
  light blue means HumaneBench. The colours only teach the grouping implicitly, by
  repetition.

---

## Progress Update as of [2026-07-28 11:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

PR #70 merged to `main` as `8d99695`. A ninth roborev pass (job 141) landed afterwards with
three findings; one was already resolved by the merge itself, the other two were stale
comments left behind by the `lacksClass` → `baseClasses` rename. Fixed those in a follow-up.

### Detail of changes made:

- **The Medium finding was true when the review ran and false by the time it was read.**
  Job 141 reviewed `645b1b8` *before* PR #70 merged, and flagged the log's "Merged to `main`
  at this point" as a false claim that would mislead a future session into skipping
  integration. It was a fair call at that moment — the line was written in the same commit
  that was still awaiting merge. Verified after the fact:
  `git merge-base --is-ancestor 645b1b8 origin/main` → yes, and `origin/main` is
  `8d99695 Merge pull request #70`. The claim is now accurate, so nothing to fix, but the
  ordering is the lesson: don't write "merged" into the artifact in the commit being merged.
- **`classesOf`'s doc block still pointed at `lacksClass`**, deleted one commit earlier. A
  reader following it looked for a symbol that no longer existed, and the described
  behavior no longer matched — the replacement also strips opacity modifiers and is applied
  by normalizing the input rather than wrapping the check in a boolean. Now points at
  `baseClasses` and states its shape.
- **`baseClasses`'s stated reach was too narrow.** It strips *every* variant prefix, not
  just responsive ones, so `hover:bg-blue-600` trips the "no fill, no pill" guard as surely
  as `sm:bg-blue-600`. The comment said "responsive variant or an opacity modifier", which
  would leave whoever hit it thinking the guard had misfired. Both the helper's doc and the
  call site now say all variants including interaction states are collapsed, and why that
  is the intended behavior on a control styled as a text link rather than a false-failure
  surface.

### Verification run

- `pnpm test` — 918 passing.
- `pnpm tsc --noEmit`, `pnpm lint` on the touched file — clean.
- `grep -n lacksClass` — no references remain.

### Tenth roborev pass (job 142) — three low findings, all applied

Still comment-only, but one of them documented a real trap:

- **`baseClasses` doesn't just fail to catch some things, it mangles others.** The header
  listed only the "out of scope, not caught" limits (a different shade, an arbitrary value)
  and not the case where the split leaves a fragment rather than a utility. Both are now
  documented.
  - **This bullet's first example was wrong and was corrected in the eleventh pass — see
    below.** `[&:hover]:bg-blue-600` normalizes *correctly*, because `pop()` keeps only the
    last `:` segment.
- **The other `baseClasses` call site still carried the narrow framing** the previous commit
  existed to remove — it named only `sm:text-center`, so tripping it with `dark:text-center`
  would give the same "the guard misfired" impression. It now points at the header and marks
  the responsive case as the one it was written for, not the only one it catches.
- **Fixed a misleading "but"** in the reach sentence ("responsive, but interaction states
  and `dark:` too"), which read as a contrast excluding responsive when the point is that
  responsive is merely the obvious case.

### Eleventh roborev pass (job 143) — one Medium, one Low, both applied

The Medium was **an error of mine in the previous commit**, and worth recording as such
since the commit's entire value was documenting a trap precisely:

- **`[&:hover]:bg-blue-600` is not mangled — it normalizes correctly.** `pop()` keeps the
  *last* `:` segment, so that token reduces to `bg-blue-600` and the guard catches it. Half
  the evidence offered for the "actively mangled" claim demonstrated the opposite of the
  claim, which would have sent a reader looking for a hole that isn't there while
  mis-locating the real one. Verified empirically this time rather than by reading the
  code — `node -e` over the actual cases:
  - `[&:hover]:bg-blue-600` → `bg-blue-600` ✅ (caught)
  - `sm:hover:bg-blue-600/90` → `bg-blue-600` ✅ (caught)
  - `bg-[url(data:image/svg+xml,x)]` → `image` ❌ (mangled — note this needs *both* splits
    to reach `image`; see the twelfth and thirteenth passes below)
  - `w-[calc(100%/3)]` → `w-[calc(100%` ❌ (mangled — the `/` split, which the header
    hadn't mentioned at all)
- **Reframed the risk as a precondition rather than a live hole.** Every guard in this file
  targets a plain utility, so no token that mangles could ever be the class one was aimed
  at. "Quietly disarms the guard for the very class it was aimed at" overstated it in the
  same direction the preceding rounds had been correcting.

### Twelfth roborev pass (job 144) — two low findings, both applied

- **The eleventh pass's correction had flattened an asymmetric rule into a single
  self-contradicting one:** "a `:` or `/` inside the *final* segment's brackets". That is
  true of the `/` case but not the `:` case — in `bg-[url(data:x)]` the colon is not *in*
  the final segment, it is what *produces* it. Restated as two separate shapes: the token's
  **last** `:` sitting inside brackets (which is exactly why `[&:hover]:bg-blue-600`
  survives — its last `:` is outside), or a `/` in whatever survives.
- **The `bg-[url(data:…)]` example had elided the substring that produces the result.**
  Tracing `split(":").pop()` over what was actually printed gives `…)]`, not `image` — the
  worked example could not be worked. Unelided.

### Thirteenth roborev pass (job 145) — two low findings, both applied

- **Even unelided, `bg-[url(data:image/x)]` → `image` was untraceable through the colon
  bullet it illustrated**, because that result needs *both* splits: `split(":").pop()`
  alone yields `image/x)]`, and only the `/` shape trims it to `image`. Swapped for a
  colon-only token whose surviving segment has no slash: `bg-[url(data:x)]` → `x)]`.
- Removed the duplicated `[&:hover]:bg-blue-600` explanation from the precondition bullet.
  The reach paragraph already settles it, and re-litigating a case that is *not* a
  limitation, inside a list whose subject is limitations, was noise.

### Fourteenth roborev pass (job 146) — two low findings, both applied

Both were defects in *this log* rather than in the code, introduced by the commit that
declared the loop closed:

- **Heading order ran 9, 10, 13, 11**, with the twelfth pass having no heading at all — its
  work was buried as a sub-bullet under the eleventh. "Thirteenth" appearing before
  "eleventh" is actively misleading rather than merely unordered, and this file is the final
  state a future session scans. Reordered, and the twelfth written up properly.
- **The eleventh-pass section still stated the colon shape as `bg-[url(data:image/x)]` →
  `image`** — the exact pairing the thirteenth pass had corrected away in the code comment.
  So the log asserted both versions with nothing marking which was current, while the
  concerns section pointed a reader at that spot for "exactly where it breaks". Annotated,
  and the pointer moved to the twelfth pass.

### Fifteenth roborev pass (job 147) — one low finding, applied

The commit fixing "the twelfth pass had no heading" reproduced that omission one pass later:
the fourteenth pass got no heading, and the stopping note still claimed the loop was rounds
nine through thirteen. So a future session would read the loop as closed at thirteen and
never learn that a further pass ran and found two real defects in the handoff artifact. The
heading-per-pass convention is this file's only index of what has been reviewed; that is
exactly why breaking it matters. Fixed by adding both missing headings — this one included,
so the record closes itself rather than needing a sixteenth pass to do it.

### Deliberate stopping point

Rounds nine through fifteen were about the wording of comments on a test helper, and then
about the wording of this log — each edit reliably producing a finding about that edit. The
findings were correct every time, and three were worth having: round seven's silently
loosened negative assertions, round eleven's worked example that demonstrated the opposite
of its claim, and round fifteen's catch that the fix for a missing heading omitted a
heading. But the value per round reached zero, so the loop was stopped by judgement, not by
running out of findings.

**A future session should not read the absence of further passes as "no findings remain."**
Read it as: comment prose in `tests/app/feedback-invite.test.tsx`, and the write-up in this
file, had reached good enough. The code itself has not been flagged since job 133.

### Potential concerns to address:

- Comment-only changes; no behavior was altered, so the mutation checks from the earlier
  rounds still stand.
- **`baseClasses` is a regex-and-split approximation of Tailwind's grammar.** If this file
  ever needs to assert against arbitrary-value classes, replace the helper rather than patch
  it — the twelfth pass above states exactly where it breaks.
- The standing item is unchanged and now the only real one: **the signer chips have never
  been observed rendering outside a unit test.** A bare worktree has no `DATABASE_URL`, so
  the count is 0, the sample is empty, and the "Recently signed by" block does not render at
  all locally. Worth an eyeball on production now that this has deployed.

---

## Progress Update as of [2026-07-28 10:45 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

Drained all five roborev findings (job 131) against the first commit. The headline one
closed the 6-fetch / 3-render trap flagged in the previous entry's concerns: the invariant
is now expressed in code rather than in two doc comments pointing at each other. The rest
tightened over-broad test assertions and restored a missing word of copy.

### Detail of changes made:

- **`RECENT_SIGNER_CHIPS` is now exported** from `src/components/SignatureMomentum.tsx`,
  and `loadSignerSample` in `src/app/page.tsx` fetches `RECENT_SIGNER_CHIPS * 2` instead of
  a bare `6`. `SignatureMomentum.tsx` has no `"use client"` and `page.tsx` already imported
  from it, so this coupling was free. The over-pull now survives refactoring without
  needing a test to defend it — this retires the top concern from the previous entry.
- **`FeedbackInvite.tsx`** — the count line read `12 comments already.` on the *Current*
  tab, where the reader is not looking at the draft and the sentence had lost its object.
  Now `12 comments already on it.` The Proposed variant had kept a full clause, so the two
  banners were inconsistent about this.
- **`tests/app/feedback-invite.test.tsx`** — added a `classesOf(html, tag)` helper and
  scoped every class assertion to the element under test. Previously
  `expect(html).not.toContain("bg-blue-600")` would have failed the moment *any* child used
  that color, and `not.toContain("text-center")` on Proposed would have failed if any
  nested element centered — both firing on changes they were not about. The button check
  now asserts against the button's own class list (no `bg-blue-600`, no `rounded-full`).
- **`tests/app/signature-momentum.test.tsx`** — the "without shrinking the row" test never
  asserted the row length, so its name was a claim the body did not make (Katherine's
  absence is guaranteed by the cap regardless of filtering). Added the `<li>` count. Also
  widened the counting regex from `/<li /g` to `/<li[ >]/g`: the old one only matched list
  items carrying attributes, so an attribute-free `<li>` would have counted as zero and
  read as a cap violation rather than a markup change.

### Verification run

- `pnpm test` — 86 files, 917 tests passing.
- `pnpm tsc --noEmit` — clean.
- `pnpm lint` on the five touched files — clean.
- PR #70 Vercel checks passed on the prior commit; re-running against this one.

### Second roborev pass (job 133) — three low findings, two applied

- **`classesOf` now throws instead of returning `""`** when the tag is absent. The
  Proposed-variant `not.toContain("text-center")` is the suite's only guard that the step
  grid stays left-aligned, and it was exactly the assertion that would have passed
  vacuously if the root `<section>` were ever renamed to a `<div>`. Also anchored it with
  a positive `toContain("rounded-xl")` on the same class list.
- **Delimited the tag name** in the `classesOf` pattern with `(?=[\s>])`. Undelimited,
  `classesOf(html, "p")` matched `<path>`, `<pre>` and `<param>` equally well — an inline
  SVG icon added to the heading would have been enough to silently retarget the assertion
  at the wrong element's class list.
- **Not applied:** the finding that `render()` and `text()` render the same tree twice.
  It is real but pre-existing — the reviewer notes the sibling test at `:157` has the same
  shape — so fixing it only in the new cases would make the file inconsistent. It costs
  one extra render of a static component and cannot change a verdict. Left for a
  suite-wide cleanup that promotes the tag-stripping half of `text` into its own helper.

### Third roborev pass (job 134) — two low findings, both applied as one change

Both findings pointed at the same soft spot, so one change closes them:

- **`classesOf` now asserts uniqueness**, not first-match: it collects all matches and
  throws unless there is exactly one. The previous throw only fired when *no* element of
  that tag carried a class — the pattern skips unclassed opening tags and advances — so
  inserting a second classed `<section>`, or a classed `<p>` above the body paragraph,
  would still have found *a* match and silently retargeted the assertion. This also
  corrects the "first-match" framing in the previous entry's concerns: the real semantics
  were "first *classed* element of that tag", which is now "the only one, or it throws".
- **Dropped the `toContain("rounded-xl")` anchor** added in the pass before. With the
  uniqueness throw in place it guarded nothing the throw did not already cover, and it
  added a false-failure surface: bumping the corner radius to `rounded-2xl` would have
  broken a test named "keeps its heading and step list intact and left-aligned", pointing
  at a class nobody had changed the alignment of. The likely repair for that is to edit
  the string, which is how a guard erodes.

### Fourth roborev pass (job 135) — two low findings, both applied

- **The uniqueness rule was already binding, not hypothetical.** The previous entry filed
  "adding a second classed `<p>` makes the assertion throw" as a *future* risk; the element
  exists today. `FeedbackInvite` renders a second classed `<p>` (the count line) whenever
  `commentCount > 0`, so `classesOf(render(current(12)), "p")` threw `found 2`. The
  centering test passed only because it happened to use the `current(0)` fixture — the
  helper was unusable against the populated state, which is the state the banner is
  normally in. `classesOf` now takes an optional discriminator
  (`classesOf(html, "p", "max-w-3xl")`), and the centering test is an `it.each([0, 12])`
  so it runs against both states. 918 tests now, up from 917.
- **Corrected the helper's doc comment**, which claimed "an inserted sibling fails the
  test" when the pattern requires `\sclass="…"` and so only counts *classed* elements. An
  unclassed wrapper still slips past. Defensible behavior, but the header read as a
  stronger invariant than the code provides — the same over-reading that produced the
  earlier "first-match" framing. Now says so explicitly.

### Fifth roborev pass (job 136) — three low findings, all applied

- **The discriminator match is now an exact class-token comparison**
  (`c.split(/\s+/).includes(d)`) instead of a substring `includes`. As a substring,
  `"max-w-3xl"` also accepted `sm:max-w-3xl` — reintroducing one layer down exactly the
  imprecision the `(?=[\s>])` tag lookahead was added to prevent.
- **The `count: 12` case now asserts something only it can see.** As written it was
  byte-identical to the `count: 0` case: the only markup that differs is the count `<p>`,
  and the `max-w-3xl` discriminator filtered that out before either assertion ran. So the
  new parameter exercised the *helper* rather than the component. It now also checks the
  count line's own class list, which exists only in the populated render.
- **Doc comment corrected again**, in a new place. It still claimed "an inserted classed
  sibling fails the test", which stopped being true the moment the discriminator was added:
  the `.filter` runs before the count, so a second classed `<p>` *without* the
  discriminator passes unnoticed. Both escape hatches (unclassed elements, and
  non-discriminator siblings) are now stated plainly.

### Sixth roborev pass (job 138) — two low findings, both applied. This one found a real hole.

- **`classesOf` now returns class *tokens*, not the raw class string.** Previous rounds made
  the discriminator an exact token match but left every *assertion* a substring `toContain`
  over the string — which is what the tests actually check. `toContain("text-center")` was
  satisfied by `sm:text-center`, `toContain("mx-auto")` by `sm:mx-auto`, `toContain("mt-2")`
  by `mt-20`. **A banner centered on desktop and broken on mobile passed the whole suite.**
  Returning an array makes every existing `toContain` / `not.toContain` exact membership,
  with no call-site changes.
  - *Verified by mutation, not by inspection:* changing `text-center` → `sm:text-center` in
    `FeedbackInvite.tsx` now fails both centering cases. It passed before this change.
- **Moved the count line's styling assertions out of the centering test** and into
  `"mentions the comment count only when there is one"`, where they belong. Asserting
  `text-zinc-600` / `mt-2` inside a test named for centering re-created exactly the
  false-failure surface that got the `rounded-xl` anchor retired two commits earlier: a
  palette tweak would have failed a centering test with a message about colors. The
  `it.each([0, 12])` stays, but its honest claim is now "centering does not depend on the
  count" rather than a smuggled-in check of unrelated markup.

### Seventh roborev pass (job 139) — one Medium, two Low, all applied

The Medium was a regression introduced by the previous commit, which is worth recording
plainly: **returning tokens tightened the positive assertions and silently loosened every
negative one, in the same direction.** `not.toContain("text-center")` against a raw string
excluded `sm:text-center`; against tokens it is exact membership, so it does not. Four call
sites were affected, including the suite's only guard that the Proposed step grid stays
left-aligned — the exact mirror of the bug the previous commit was written to fix.

- **Added `lacksClass(tokens, base)`**, which strips `variant:` prefixes before comparing,
  and used it for all four negatives (the two "no fill, no pill" button checks and the
  Proposed left-alignment guard). Positives keep exact token membership.
- **Doc comment now states both directions** of the string→tokens tradeoff and points at
  `lacksClass`. Every previous round corrected this header for claiming a stronger helper
  than the code delivered; this time the omission was the sharper edge, since four of the
  call sites are negatives.
- **Replaced the count line's vacuous `not.toContain("text-center")`** — a class that has
  never been on it and that nothing pushes toward it, so the assertion had no failure mode.
  The test now asserts the *section* carries `text-center`, which is what the count line's
  centering actually inherits from, giving the stated intent something real to break.

*Verified by mutation, both directions:* adding `sm:text-center` to the Proposed section
fails the left-alignment test, and re-adding the pill as `sm:bg-blue-600 sm:rounded-full`
fails the text-link test. Both passed before this commit.

### Eighth roborev pass (job 140) — three low findings, all applied

Two of them folded into one change: **`lacksClass` became `baseClasses`**, which normalizes
tokens (`sm:hover:bg-blue-600/90` → `bg-blue-600`) and lets the negatives stay real
`not.toContain` matchers.

- `expect(lacksClass(...)).toBe(true)` collapsed every failure to `expected false to be
  true`, hiding *which* variant slipped in — on three load-bearing guards, the ones you most
  want legible when they fire. Normalizing the input instead of wrapping the check in a
  boolean keeps the matcher's diff.
- `lacksClass` stripped `variant:` prefixes but not opacity modifiers, so `bg-blue-600/90` —
  the most likely form of a re-added fill in current Tailwind, and visually identical to the
  pill this guard exists to exclude — passed. `baseClasses` strips both. Its reach is now
  stated in the doc comment: a different shade (`bg-blue-500`) or an arbitrary value
  (`bg-[#2563eb]`) is still out of scope.
- **Dropped the section `text-center` assertion added to the count test last round.** It was
  byte-identical to the one in the `it.each([0, 12])` centering test, on the same
  `render(current(12))` markup, so it had no independent failure mode — the previous vacuous
  assertion was replaced with a duplicate rather than a new observation. The count line's
  inherited centering genuinely has nothing on the count line to guard; the comment now says
  so and points at the test that does cover it.

*Verified by mutation:* `sm:text-center` on the Proposed section fails the alignment test,
and `bg-blue-600/90 rounded-full` on the link fails the text-link test — the latter would
have passed under `lacksClass`.

### Status

Eight roborev passes; jobs 131, 133, 134, 135, 136, 138, 139 and 140 all triaged and closed.
Rounds three through eight were about the precision of the class-assertion helpers — the
feature code in `FeedbackInvite.tsx`, `SignatureMomentum.tsx` and `page.tsx` has not been
flagged since job 133. The helper rounds were not busywork: round six caught assertions
that would have passed a broken mobile layout, round seven caught the regression round six
introduced while fixing it, and round eight caught a fill form the round-seven fix missed.

This is the point at which the work was merged — see the entry above for the merge commit
and for the ninth pass that landed just after it.

### Potential concerns to address:

- The signer-chip behavior still has no end-to-end verification — see the standing note in
  the entry below. Nothing here changed that.
- **`classesOf` has two documented blind spots**, both accepted rather than fixed: it
  cannot see elements with no `class` attribute, and with a discriminator its uniqueness
  check covers only elements carrying that discriminator. Closing either properly means
  parsing a real DOM instead of running a regex over static markup, which is more machinery
  than this one banner justifies. The helper's doc comment states both.
- `tests/app/signature-momentum.test.tsx` renders each tree twice, once via `render` and
  once via `text`. Flagged by roborev and consciously left: it is the file's existing
  pattern (the sibling test does the same), it costs one extra render of a static
  component, and it cannot change a verdict. Worth folding into a shared strip-tags helper
  next time that file is touched.

---

## Progress Update as of [2026-07-28 10:30 Pacific]
*(Most recent updates at top)*

### Summary of changes since last update

First entry on this branch. Two homepage presentation fixes: the "Recently signed by"
pill row now shows three signers instead of six so it sits on one line, and the blue
"This is a living document" banner above the document tabs was shortened, de-em-dashed,
centered, and had its filled blue button demoted to a text link. Added the branch's
first coverage for `FeedbackInvite`, which had none.

### Detail of changes made:

- **`src/components/SignatureMomentum.tsx`** — introduced `RECENT_SIGNER_CHIPS = 3`
  (module-level const, near `MomentumSigner`) and used it for the `.slice()` in
  `SignatureMomentumPanel` that previously hardcoded `6`. Layout was already
  `flex flex-wrap justify-center`, so no class changes were needed: three chips fit
  one line at desktop width and still wrap on a narrow phone rather than clipping names.
- **`src/app/page.tsx`** — `loadSignerSample` intentionally still fetches
  `listSignatures(null, { limit: 6 })`. The over-pull is not a leftover: the panel
  filters out signers whose `displayName` is blank, so fetching exactly three would let
  two anonymous signers collapse the row to a single chip. Added a doc comment saying
  this, because the 6-vs-3 mismatch now reads like a bug to anyone scanning it.
- **`src/components/FeedbackInvite.tsx`**, `variant === "current"`:
  - Heading `This is a living document — you can change it.` →
    `This is a living document. You can change it.`
  - Body cut from five sentences to two. Dropped the "voted up or down" and
    "not a reason to walk away" beats.
  - `text-center` added to the `<section>`; the paragraph gained `mx-auto` alongside its
    existing `max-w-3xl`. Without `mx-auto` a width-capped paragraph stays pinned to the
    left edge of a centered box — this is the non-obvious half of "center the box".
  - The filled `rounded-full bg-blue-600` button became an underlined blue text link.
    It is still a `<button>`, not an `<a>`: it calls `onOpenDraft` to switch tabs in
    place rather than navigating, so link *semantics* would be wrong even though link
    *styling* is what was wanted.
  - Link text is new (`Mark up the v{proposedVersion} draft →`) because both prior
    strings were removed by request — the old button label and the
    `No comments yet — yours would be the first.` empty state.
  - The comment count line now renders only when `commentCount > 0`, as
    `{n} comments already.` At zero the box shows nothing there.
- **`src/components/FeedbackInvite.tsx`**, `variant === "proposed"` — em dashes only, by
  request. Heading now `You're in the draft. This is where feedback happens.` and
  `STEPS[0].detail` now `Drag across a sentence. On a phone, press and hold.` Its
  `sm:grid-cols-3` numbered step list is deliberately left un-centered.
- **`tests/app/feedback-invite.test.tsx`** (new) — `FeedbackInvite` had zero coverage.
  Asserts no U+2014 in either variant, `text-center` present on Current and absent on
  Proposed, `mx-auto` + `max-w-3xl` both on the paragraph's class list, the new link text,
  absence of the two removed strings, and the count line's presence/absence at 12 vs 0.
- **`tests/app/signature-momentum.test.tsx`** — grew the shared `sample` fixture from 3 to
  5 named signers. This mattered: at exactly 3 the new cap assertion would have passed
  vacuously against the old `.slice(0, 6)`. Added a case asserting exactly three `<li>`
  render and the 4th/5th names do not, plus one covering a blank-display-name signer
  being filtered without shrinking the row.

### Verification run

- `pnpm test` — 86 files, 917 tests, all passing.
- `pnpm tsc --noEmit` — clean.
- `pnpm lint` on the five touched files — clean. (Repo-wide `pnpm lint` reports 138
  pre-existing `@typescript-eslint/no-explicit-any` errors in `tests/server/**` and
  friends, none in files touched here.)
- `pnpm dev` + a real browser load of `/` — confirmed the box renders centered with the
  two-sentence copy, the underlined link, and zero em dashes inside the section.

### Potential concerns to address:

- **The 6-fetch / 3-render split is a real trap.** It is commented on both sides now, but
  anyone "tidying" `loadSignerSample` down to `limit: 3` will silently reintroduce the
  short-row bug, and no test catches it — the panel test passes a fixture directly and
  never exercises the query. A test over `loadSignerSample` would need DB mocking.
- **`pnpm build` cannot complete in a bare worktree.** There is no `.env.local`, so the
  export step dies on `/_not-found` with `@clerk/clerk-react: Missing publishableKey`,
  preceded by `DATABASE_URL is not set`. Compilation itself succeeds
  (`✓ Compiled successfully`), and `tsc --noEmit` is clean, so this is environment-only —
  but it means a local `pnpm build` is not a usable gate here.
- **The signer chips could not be verified in a live browser** for the same reason: with
  no database the count is 0 and the sample is empty, so the "Recently signed by" block
  does not render locally at all. Its three-chip behavior rests entirely on the unit
  tests. Worth an eyeball on a preview deploy.
- **Chips are gated on `RAW_COUNT_THRESHOLD`** (5000, overridable via
  `NEXT_PUBLIC_SIGNATURE_COUNT_THRESHOLD`). Above it, `SignatureMomentumPanel` early-returns
  the "scale" branch and the recent-signers row disappears entirely — the three-signer
  change is only ever visible below that threshold.
- **Em dashes remain elsewhere on the homepage** and were left alone as out of scope:
  the hero line in `src/app/page.tsx`, the `Version X — a living document` footer eyebrow,
  and its twin in `src/app/v/[version]/page.tsx` (which uses `&mdash;`). If the intent is
  a site-wide dash purge, those are the next targets.

---
