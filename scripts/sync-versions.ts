// Stub. Real implementation lands in Task 7.
// Exists so the package.json `postbuild` hook does not break the build
// in the intermediate tasks 2 through 6.
console.log("sync-versions: stub (no-op until Task 7 implements it).");
